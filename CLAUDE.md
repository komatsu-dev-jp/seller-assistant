@AGENTS.md

# Claude Code固有の補足

- `AGENTS.md` をCodexとClaude Codeの共通ルールの正本として扱います。
- 作業開始時に、関連仕様、`memory/INDEX.md`、`docs/DECISIONS.md`、`handoffs/active/` を確認します。
- 同じファイルを別エージェントと同時編集しません。担当範囲、ブランチ、worktree（分離した作業フォルダ）を申し送りへ記録します。
- 作業終了時は、変更ファイル、検証結果、未解決事項、次の具体的な一手を申し送りへ残します。
- 複数作業で再利用でき、検証済みの知見だけをmemory候補にします。
- APIキー、トークン、Webhook URL、個人情報、生ログを保存しません。
- 認証、課金、データ移行、公開操作、破壊的変更は、明示された権限と承認なしに実行しません。

