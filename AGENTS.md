# フリマ物販業務アプリ 共通開発ルール

このファイルを、CodexとClaude Codeが共通で守る開発ルールの正本（最優先で参照する原本）とします。

## コミュニケーション

- 結論から、初心者にも分かる言葉で説明します。
- 専門用語を使うときは、初出で短い説明を添えます。例: `worktree`（同じGitリポジトリを別フォルダで安全に作業する仕組み）。
- 「完了したこと」「確認した方法」「未確認のこと」を分けて報告します。
- 推測を事実として扱わず、不明な点は「未確認」と明記します。

## 作業開始時の確認

- 対象パスに適用される `AGENTS.md` を上位から順に読みます。
- `memory/INDEX.md`、関連する `docs/DECISIONS.md`、`inbox/`、`handoffs/active/` を確認します。
- 既存ファイル、未完了作業、変更禁止範囲、外部操作の権限を確認してから編集します。
- ユーザーや別エージェントの変更を保護し、依頼と無関係な編集をしません。

## 一つの窓口と担当分け

- ルートCodexをユーザーとの一つの窓口にします。専門担当は、範囲を限定した内部作業だけを受け持ちます。
- 小さな確認や1ファイルの軽微な変更では、担当を増やしません。
- 複数担当へ分けるのは、独立した作業が2つ以上あり、並行でき、同じファイルを編集せず、成果と確認方法を個別に定義できる場合だけです。
- 同じファイルには、同じ時点で一人の書き込み担当だけを置きます。
- 担当交代や長時間作業では `handoffs/active/` に申し送りを残し、完了後は `handoffs/archive/` へ移します。

## 仕様・判断・保留事項

- 合意済みの製品・技術判断は `docs/DECISIONS.md` に日付付きで追記します。過去の記録は書き換えず、変更時は新しい項目から以前の判断を参照します。
- 担当者の判断が必要で、次の作業を止める事項だけを `inbox/` に保存します。一時メモや会話全文は保存しません。
- Notion仕様が正本として指定されている機能では、実装前に該当仕様とローカル成果物の整合を確認します。
- 仕様、採用デザイン、受け入れ条件に矛盾があれば、勝手に決めずルート担当へ戻します。

## 製品と外部連携の安全境界

- 外部サービスへは、公式に提供された画面、CSV、または契約・承認済みAPIだけで連携します。
- スクレイピング（Web画面から機械的に情報を抜き取る方法）、RPA（人の画面操作を機械で代行する方法）、非公開API、無人の自動出品・自動値下げ・一括更新は実装しません。
- 公開、出品、価格反映、承認、会計確定など重要な操作には、人が内容を確認する工程を残します。
- AIの文章・画像・仕訳は候補として扱い、根拠と確認状態を示します。AIだけで確定しません。
- 税務機能は記録整理と会計ソフトへの受け渡しを支援する範囲に限定し、個別の税務助言や確定申告完了を表示しません。
- Slack・Notionへの送信、GitHub作成、公開、本番反映、PRマージ、課金、破壊的変更は、対象と権限が明示された場合だけ行います。

## 秘密情報と個人情報

- APIキー、アクセストークン、Cookie、パスワード、Webhook URL、個人情報、生ログ、実データ行をGit、`memory/`、`handoffs/`、`inbox/`、`docs/DECISIONS.md`へ保存しません。
- 住所、原価、売上、口座、税務資料は必要最小限の権限で扱い、画面・ログ・テストデータへ不用意に露出させません。
- 実データが不要な検証では、架空データを使用します。

## memoryの扱い

- `memory/` は、チームが確認済みの再利用可能な知識を置く場所です。AIの再学習データではありません。
- 重大または再発し得る失敗は、まず `status: candidate` のincident（問題の候補記録）として残します。
- 原因、予防策、検証証拠、重複確認、独立レビューがそろった場合だけlesson（再利用できる教訓）へ昇格します。
- `memory/INDEX.md` には `status: active` のlessonだけを載せます。途中経過や未検証の推測は載せません。

## 実装と検証

- 現時点では、実装方式や `package.json` などのpackage manifest（依存関係と実行コマンドを定義するファイル）が未確定です。存在しないlint・test・buildコマンドを作りません。
- Goal開始時に実際のpackage manifestと技術構成を確認し、この節へ正しいセットアップ、lint、test、build、安全確認コマンドを追記します。
- コードや設定を変更した場合は、影響に応じた実際の検証を行い、コマンド、結果、未確認範囲を報告します。
- ドキュメントだけの変更では、対象パス、Markdown構造、参照先、機密情報の不在を確認します。
- 実行できなかった検証を成功扱いにしません。

## Gitと変更保護

- Git未導入の状態を、許可なく `git init` しません。
- Git導入後、通常の実装を `main` へ直接行わず、確認済みのブランチとworktreeを使用します。
- 既存変更を消す `git reset --hard` や一括上書きは行いません。
- コミット、push、Draft PR、PRマージは、それぞれ依頼された範囲と確認済みリポジトリに限ります。

