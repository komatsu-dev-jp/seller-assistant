# PC版Web・Slackコメント反映 v4

- 作成日: 2026-08-27（JST）
- Slack親TS: `1787754933.967639`
- 対象: PC03 v3へ付いたバーコードに関する修正コメント1点だけ
- 状態: 修正版をSlackへ送信・再読済み。ユーザーの事前指示に基づきGoal再開
- 位置づけ: 承認済みのPC版デザインへ加える限定修正。実装済み画面ではない

## 変更しない画像

- PC02 v3とPC08 v3は新しい修正コメントがないため承認済みとして固定する。
- PC07 v2はSlackで`問題なし`を確認済みのため変更しない。
- PC01、PC04、PC05、PC06、PC09、PC10、PC11、PC12、PC13は修正コメントがないためv1を維持する。
- PC03も、バーコードとスマホ読取導線以外の画面目的、配置、文言、安全境界を維持する。

## PC03 商品ごとのバーコードとスマホ読取

- 中古1点ものは、登録した商品ごとに短い`商品・在庫番号`と一意のバーコードを1組作る。
- A4 24面では、24商品の各ラベルへ異なる短い番号と、その番号に対応するバーコードを表示する。
- スマホでバーコードを読み取ると、該当する商品と現在の保管場所をアプリ内で開ける。
- 読取は商品を探す入口であり、棚入れ、移動、出庫を自動確定しない。最後は人が内容を確認して保存する。
- 外部サービス、外部API、自動ログイン、スクレイピングを使わず、PWA（ホーム画面へ追加できるWebアプリ）のカメラ読取として扱う。
- バーコードの規格はモック段階では固定しない。実装時に短い番号、印刷の読みやすさ、端末カメラの対応を確認して決める。
- 手書きラベルを無料の標準として残し、印刷とバーコード読取は任意とする。

## 画面別の限定修正

- 画面09: 短い番号`0123`の下へバーコード例を追加し、`スマホで読み取ると、商品と保管場所を開けます`と説明する。
- 画面10: A4 24面の全ラベルへ、異なる短い番号と個別バーコードを表示する。`登録商品24点 / ラベル24枚 / 同じ番号なし`は維持する。
- 画面11: 変更しない。
- 画面12: `商品・在庫番号 0123`へ対応するバーコードを表示し、保管場所の二重確認と人による確定は維持する。

## Goal再開の扱い

- ユーザーの`修正箇所は少ないので修正後goalを実行してください`を、PC03 v4の限定修正後に現行Goal契約を再開する指示として記録する。
- PC03 v4を同じSlackスレッドへ送信して再読した後、承認済みモバイル版・PC版を仕様と受け入れ条件へ統合し、実装・検証を再開する。
- 公開、本番反映、PRマージは行わない。Goalの到達点は、現行契約で定めた検証済み実装とDraft PRまでとする。

## Slackコメントの確認結果

- PC03 v3 TS `1787809580.401509`: 一括印刷からバーコードが消えている点と、スマホでバーコードを読み取って商品を探す導線の必要性について修正コメントあり。
- PC02 v3 TS `1787809552.702339`: 新しい修正コメントなし。
- PC08 v3 TS `1787809608.295459`: 新しい修正コメントなし。
- スレッド: <https://p-evidence.slack.com/archives/C0BPZCB25T3/p1787754933967639>

## 画像QA結果

- PC03 v4は1536×1024で、画面09〜12と2×2配置を維持した。
- 画面09と12に番号`0123`に対応するバーコードを表示した。
- 画面10の24ラベルすべてに、異なる短い番号と個別バーコードを表示した。
- `読取は商品検索だけ。移動は確認後に保存します`を表示し、外部APIや自動確定を示していない。

## Slack送信結果

- PC03 v4: file ID `F0BSM3XPRQF`、TS `1787818024.476329`
- 送信後に同じスレッドを再読し、ファイル名、file ID、説明、TSを確認した。
- ユーザーから修正後のGoal再開指示があるため、追加の待機を挟まず現行Goal契約の実装・検証へ進む。
