# 2026-09-24 公開モバイルの操作不具合

利用者の10枚の画面を対象に、静的な見本のままだった操作を修正する。製品全体の完成や実業務への接続とは区別する。承認済みのカード、配色、フッターを再利用し、操作部品と状態表示だけを追加する。

## 境界

- GitHub Pagesの確認版だけ。API、DB、実商品、税務確定、自動価格変更、有料サービスは変更しない。
- 文字の確認結果は専用のsessionStorage（同じブラウザーのタブ内の一時保存）に保存する。既存の検品・撮影データを上書きしない。
- 画像の比較は画面内だけ。画像・ZIPの外部送信、自動加工は追加しない。ZIP未対応は表示する。
- 会計は架空例の操作確認。資料や重複の確認を自動で合格扱いせず、実際に行った操作だけを履歴へ載せる。
- 計画・実装: ルートCodex（実モデル設定は未確認）。独立監査・凍結差分レビュー: 別実行Astra low。mode: astra-centric。費用・トークンは未計測。

## 受け入れ条件

- [x] 39: 分類・担当を選べて、該当する確認画面または分類別の見本詳細を開く。
- [x] 40: チェックと理由を入力し、3秒の長押しが成立した場合だけ確認用の仮状態を記録。途中で離す・画面を離れる場合は中止。長押し時間と中断は本番関数を動かすテストで検証し、ブラウザーでは前提入力と短いクリックで記録されないことを確認。
- [x] 44〜48: 設定、候補選択、本人による事前確認、見本ファイル、手動取込結果がつながる。未確認のまま成功しない。
- [x] 49: 自分が操作した確認用履歴だけを表示し、詳細を開ける。架空の過去の成功履歴は出さない。
- [x] photo-01: 写真を開いて確認でき、操作していない写真を確認済みにしない。
- [x] photo-07: 実際に画像を選べ、色・ロゴ・傷の確認を本人が操作。原本を上書きせず、ZIP不可・再読み込みで画像が消えることを明示。
- [x] box-02: 加算・減算と確定数が一致し、0未満にならない。後続の未実装箱処理へ完了したふりで進まない。
- [x] sales-06: 公式商品URLがない場合は入力を案内し、検証済みのURLだけを本人が開く。コピー失敗を成功扱いしない。通知がボタンを塞がない。
- [x] 自動テスト、全体check、review build、サイドパネル実操作、独立レビューを実施する。

対象外: 実端末Safariの実機検証、会計ソフトでの実取込、実出品・値下げ、P1全体の業務保存。今回の確認版修正をこれらの完了として報告しない。

## 検証証拠

- `npm run check`: 120ファイル・951テスト、整形・lint・型・coverage・秘密情報検査・API/Webビルド合格。その後、独立レビュー指摘のCSV再試行テスト1件を追加した。
- `npm test` → `npm run lint` → `npm run build`: 最終差分を再実行。120ファイル・952テスト。
- `REVIEW_BASE_PATH=/seller-assistant npm run build --workspace @resale/review`: 135ページ、138書き出しファイル。全12対象画面へ到達。
- 右サイドパネル390×844で、box48→49→確定→再読み込み49、44の仮設定→45売上/送料選択→46本人チェック→47見本→48「取込していない」→49履歴の詳細を実操作した。
- 写真選択で見本PNGを読み込み、比較画像表示、色・ロゴ・傷の本人チェック、1/5枚確認を実操作。photo-01ではブランドタグ拡大と1/5枚確認、全6画像の読み込み成功を確認した。
- sales-06のコピー成功、本人が変更していない結果の一時記録を確認。外部商品ページは開いていない。
- 下部固定操作と本文の重なりを修正。12画面すべてで本文下端610px、操作上端613px、横はみ出し0、画像エラー0、console warning/error0。
- 独立Astra lowレビュー: 初回P2（未保存時のCSVクリック抑止と成功記録の不整合）を修正し、42対象テストを別実行してP1/P2 0件。
- 未確認: 実iPhoneの3秒長押し、Safari固有の写真選択、CSVの実ファイル保存先。CSVの生成・開始操作・履歴は確認したが、サイドパネルのdownloadイベントは時間切れとなったため、端末への保存完了とは報告しない。

公開版の検証結果はPRとこのタスクの最終報告に紐付ける。実業務との接続や全体Goalの完了率へ換算しない。
