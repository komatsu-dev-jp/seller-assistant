# 全ページの操作と読みやすさを直す

開始日: 2026-09-20（JST）。状態: 実装中（未完成）。最終対象は全127画面、作業順は初回90画面→残り37画面で確定。検品・会計・場所写真審査の主要仕様は回答待ち。基準: `ee02350`。

## 完成条件

### 最新の全体確認

2026-09-20、主要未接続を別担当が再調査。現在確認できる主要不足は下記の仕様判断に依存し、無条件に追加実装できる主要経路は確定できなかった。確認済み機能の細かい再試験だけを繰り返して完成扱いにしない。利用者の「モバイル版もPC版も全ページを忠実に再現」という指示を、最終対象127画面の決定として採用した。初回90画面を先に安定させ、その後37画面を同じ完了基準で仕上げる。ROOTの `output/all-pages-issue-current.md` に初心者向け公開本文を再整理した。

利用者の依頼は、Web・モバイルの押せない操作と文字の重なりを直し、実際のブラウザで全ページの操作を確認すること。画面が表示されるだけでは合格にしない。承認デザイン、無料運用、人による重要操作の確認を維持する。

### 2026-09-20 最新本番buildの承認モック全127画面

- [x] 最新の本番用buildをPC内の `127.0.0.1` だけで一時起動し、スマホ75画面・PC52画面を同じChromeで127/127撮影。確認後は一時サーバーを停止。
- [x] 127/127で期待した画面サイズ、縦横はみ出し0、画面外へ切れた操作部品0、外部resource 0。撮影報告は `output/playwright/all-pages-prod-127-20260920/capture-report.json`。
- [x] 承認原画像と現在画面の全127組・比較シート25枚を生成。入口・ホーム・発送・PCホーム・PC発送の代表シートを目視し、開発用の黒いツールボタンがない本番buildで再確認。証拠は `output/playwright/approved-ui-comparison/prod-127-20260920/`。
- [ ] これは承認モックの表示確認であり、P1の37画面を業務APIへ接続済み、または全操作を保存まで確認済みという意味ではない。業務操作の完成条件は下のまま維持する。

## 確認できた原因

### 2026-09-20 割当0件でも押せる読取・撮影入口

- [x] 架空の現場担当者で、格納待ち0件・利用可能な場所0件でも「商品と場所を読み取る」が青いリンクとして押せる状態を実ブラウザで再現。遷移後に商品番号を入れて初めて一般的な「商品が見つかりません」と表示され、割当が必要だと分かりにくかった。
- [x] 商品と保管場所の両方が1件以上そろった時だけ読取リンクを有効化。読込中、両方なし、商品だけなし、場所だけなしを個別の日本語案内に分け、発送担当の割当0件も同じ基準で無効化。
- [x] 無効時はネイティブの無効ボタン、灰色背景、右端「—」で表示し、押せる矢印を残さない。390×844の実ブラウザで無効状態、理由文、横はみ出しなし、コンソールエラー0件を確認。証拠 `live-no-assignment-action-disabled-mobile-final.png`。
- [x] 下部メニューなどから読取URLを直接開いた場合も、割当0件では入力欄を表示せず「先に割当が必要です」と理由・戻り先を表示。390×844で文字重なり、横はみ出し、コンソールエラーがないことを確認。証拠 `live-no-assignment-scan-guard-mobile.png`。
- [x] 撮影担当0件でも「割当商品の撮影・採寸」が押せたため、既存のPC内 `capture-tasks` で担当件数を確認し、担当商品がある時だけリンクを有効化。読込中・0件・確認失敗は理由付き無効ボタンとし、外部APIは追加していない。
- [x] 架空の担当0件ユーザーで撮影入口が無効、右端「—」、390×844で横はみ出し・文字重なり・コンソールエラー0を確認。証拠 `live-no-capture-assignment-disabled-mobile.png`。
- [x] 画面末尾で下部メニュー、注意欄、ログアウトの位置を実測。下部メニュー上端780pxに対しログアウト下端722pxで、内容を隠していないことを確認。
- [x] 状態判定と直接遷移防止のテストを追加。撮影担当一覧の再読み込みが重なった場合は、古い応答で担当画面や読込状態を戻さない要求番号の確認も追加。全体check session14873 exit0。661テスト・73ファイル、整形・静的解析・型・security・Web/API buildすべて合格。

### 2026-09-20 返品の失敗時のやり直しと廃棄候補

- [x] 返品受取の確認チェックなしではPOSTしない。確認後の通信失敗503で最新一覧へ戻り、対象を選び直して受取保存成功。
- [x] 別の商品番号では先へ進まず、正しい番号で進む。通常棚は返品隔離先として拒否し、返品専用場所で隔離保存成功。
- [x] スマホ幅で検品保存を503失敗→一覧から再選択→検品結果を再確認→保存成功。「廃棄候補・未確定」として残り、廃棄や再販売を自動確定しない。
- [x] 再読込後も保存結果を再表示。PC/スマホの横あふれなし、スマホ画像を目視。証拠 `live-return-dispose-retry-mobile.png` / `live-return-dispose-retry-pc.png`。今回ソース変更なし、直前check62397の656テスト合格版で実操作。
- [x] 別の架空商品0005で受取・隔離・検品の各POSTを実保存成功後に応答だけ切断。一覧再取得から再選択すると次工程または保存結果へ進み、同じ操作の保存画面へ戻らないことを確認。通常棚へ戻す前は場所登録待ちを維持。
- [x] 返品隔離保存POSTだけを503にし、最新一覧へ戻って再選択できること、在庫が発送済み・移動履歴番号2・場所未登録のまま変わらないことを確認。証拠 `live-return-quarantine-503-safe-pc.png`。
- [x] 同じ返品を2画面から確定し、先の画面だけ200で隔離、遅い画面は実API 409で拒否。移動履歴は2→3の1回だけ、隔離先 `UI-RETURN-7`、画面は日本語で状態変更と再選択を案内。証拠 `live-return-quarantine-409-japanese-pc.png`。

### 2026-09-20 スマホ在庫カードの右端切れ

- [x] スマホの2列カードにPC用の最小幅620pxが残り、商品識別番号と状態が右端で切れる状態を実画像で確認。
- [x] モバイル限定で最小幅を解除し、2列の幅をカード内に収める。320/390/1440pxで行と各項目の境界を実測して合格、390px画像も目視確認。PC配置は維持。
- [x] 独立レビュー合格。画面証拠 `live-inventory-card-width-320.png` / `390.png` / `1440.png`。
- [x] 集計修正とカード幅修正を含む最終check62397 exit0。656テスト・72ファイル、整形・静的解析・型・security・Web/API buildすべて合格。

### 2026-09-20 2名で行う棚卸の実操作

- [x] 架空オーナーで棚卸開始・初回計数を提出。本人には再確認操作を出さず、自己承認ボタンが無効で別担当への案内が表示されることを確認。
- [x] 別の架空在庫担当でログインし、同じ差異を引き継ぎ。商品・棚の再読取、写真と確認メモの保存、3秒長押し、不足候補の確定、棚卸承認まで成功。
- [x] 再読込後の承認履歴を選択し、保存済みの不足候補と復元画面へ到達。実商品は使っていない。
- [x] 2名モードの承認済み棚卸で商品・棚を再確認。復元POSTに503を1回注入してボタンが再度有効になることを確認し、同じ長押し操作の再試行で実復元成功。再読込後の履歴と在庫一覧の「在庫あり」まで確認。
- [x] 復元済みも未解決として集計する不具合を発見。仕様は未解決件数なので、終了状態resolved/restoredを除外し、表示を「未解決の差異」へ変更。履歴・在庫自体は変更しない。独立レビュー合格、実ブラウザで1→0と在庫あり保持を確認。
- [x] 同じワークスペースに復元済み差異3件を保持したまま、新しい2名確認棚卸で2/3点を読み取り、1件を再確認待ちにした。実APIと在庫画面は未解決だけを1件と集計し、在庫3点の状態・場所は維持。PC/390pxで横あふれなし。証拠 `live-mixed-restored-unresolved-summary-pc.png` / `live-mixed-restored-unresolved-summary-mobile.png` / `live-mixed-unresolved-stocktake-pc.png` / `live-mixed-restored-history-pc.png`。
- [ ] 返品の今回以外の追加異常系、実端末カメラ等は残る。

### 2026-09-20 商品ラベル再発行の実操作

- [x] 進行中棚卸の対象外で未格納の架空商品0006だけを選び、破損理由の再発行POSTを通常画面から1回保存。本文は `targetType=inventory_unit`、正しい在庫ID、`reasonCode=damaged`、`humanConfirmed=true` で、201応答はV2。
- [x] 再取得後も商品番号 `INV-000006-2`、未格納、`putaway_pending`、撮影完了状態を維持し、ラベル版だけV1→V2。印刷は行わず、PC/390pxの1枚プレビューと横あふれ0を確認。
- [x] 再発行成功後に画面案内がなかったため、実201応答の対象種別・コード・版を使って成功を表示。商品は「商品番号・ラベル」で新版確認、場所は場所コード付きで新しい場所ラベル利用を案内。
- [x] 初回独立レビューの「場所にも商品用確認先を案内する」P2を修正し、商品/場所の2文言を単体テスト。再レビューは指摘なしPASS。
- [x] 案内の画面確認はPOSTをAPIへ送らない一時201応答で商品・場所の両方を行い、PC/390pxで全文表示を確認。模擬通信を解除し、実データが商品V2・場所V1のまま、通信ルート0件であることを再確認。
- [x] 最終 `npm run check` は74ファイル・668テスト、整形・lint・型・coverage・秘密情報検査・依存関係検査・API/Web build（86 routes）まで合格。
- [ ] 古いV1ラベルを実端末カメラで読んだ場合の拒否は未確認。実端末カメラ項目に残す。

### 2026-09-20 オフライン保存と同期後の一覧更新

- [x] 通信を切って架空ニットの格納を保存し、送信待ち1件として保持。503の通信失敗後も消えず、再試行で実保存成功・送信待ち0件を確認。
- [x] 同期成功後もホームの格納待ちが1件のまま残る不具合を修正。同期・割当解除の通知でモバイルと管理ホームを再取得する。古い応答による上書きも抑止。
- [x] 独立レビューの管理ホーム応答競合P2を修正し、再レビューPASS。ブラウザで古い一覧を画面上に模擬表示し、同期通知だけで最新の0件へ更新されることを確認。模擬通信は解除済み。在庫への追加書込みなし。
- [x] check3390 exit0。653テスト・71ファイル、整形・静的解析・型・security・Web/API build合格。画面証拠は `live-offline-summary-refresh-mobile.png`。
- [x] 修正後の新規オフライン保存→再接続→作業ホーム同期待ち1/格納待ち1→同期→両方0/次作業なしまで再読込なしで実再走成功。対象は返品検品後の0005。証拠 `live-offline-full-sync-refresh-mobile.png`。
- [x] 管理ホームで同期通知を2回発生させ、最初の応答を保留して2回目を先に返す制御試験を実施。新しい件数12の表示後に古い91を返しても12を保持。模擬解除・再読込済み、業務データ書込みなし。証拠 `live-home-refresh-race-pc.png`。
- [x] PCホームで最初の在庫概要GETを保留し、2回目の実200を先に表示した後、最初の応答だけ503にした。最新の売上・利益・確認表示を保持し、エラー表示なし。スマホ作業ホームでは再取得GETの保留中に別画面へ退出し、古い503を返してから再入場。格納待ち0件・場所1件を正常表示し、エラーなし・横はみ出しなし。模擬通信0件。証拠 `live-home-stale-failure-pc.png` / `live-mobile-exit-stale-failure-final.png`。
- [ ] 実端末カメラと、今回とは異なる古いエラー条件・画面退出条件は未確認。全ページ完成と扱わない。

### 2026-09-20 全体完成を妨げる残項目（現状監査）

次の項目が残るため、テスト合格や表示成功だけで全ページ完成にはしない。

| 優先 | 次に必要なこと                                                   | 区分                 | 合格に必要な証拠                                                            |
| ---- | ---------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------- |
| 1    | 最終対象を全127画面とし、初回90画面→残り37画面の順で完成させる   | 決定済み             | 利用者の全ページ忠実再現指示を採用し、Issue・計画・申し送りへ同じ範囲を記録 |
| 2    | 発送済み注文の不足金額・税区分・0円の扱いを決め、会計CSVまで接続 | 未実装・回答待ち     | 同一商品で不足入力→人の確認→CSV。原記録を上書きしない                       |
| 3    | 商品種類別の検品と細部写真の保存を実装                           | 未実装・回答待ち     | 必須/任意とパンツ/スカート分岐の承認、保存・再表示・拒否の試験              |
| 4    | 場所写真を承認する前の審査方法を決定                             | 仕様矛盾・回答待ち   | AC-047/TA-033の例外を明示承認し、撮影者以外の許可担当だけで審査できること   |
| 5    | 全画面の操作ごとの合否を完成                                     | 実装済み部分の未検証 | 実端末カメラ、残る条件別操作、全127画面の操作証拠                           |

根拠: `approved-ui-runtime-route-map.md`、`approved-screen-scope.ts`、`mobile-screen-data.ts`、`accounting-missing-facts-packet.md`、P12提案、製品AC-047/技術TA-033。2026-09-20に別実行の読み取り監査と照合。最終対象は全127画面で確定したが、P1の見本37画面を実装済みとは扱わない。Project #1のStatusはIn Progressを確認済み。Issue #11本文は `output/all-pages-issue-current.md` へ更新済みだが、実装状況・検証用ID・未完了項目をGitHubへ公開する明示承認がないため外部編集は行わない。GitHub側を更新済みとは扱わない。

- Pagesは `apps/review` の静的な確認版。実保存する `apps/web` の業務画面とは別である。
- モバイルの主操作、PCの共通主操作は画面番号順のリンクが中心。保存と表示されても保存しない。
- パスワード表示など、単独で実装できるボタンも無反応。
- 公開PCホームで売上金額とアイコンが重なることを実ブラウザで確認。
- P1の見本37画面を、P0実装済みとして扱わない。既存の対応表を参照する。

## 実装パケットと確認

| 順番 | 作業                                                                | 完了の確認                                                     | 状態   |
| ---- | ------------------------------------------------------------------- | -------------------------------------------------------------- | ------ |
| A    | 再現できたPCホームの重なり、モバイルのパスワード表示を修正          | 画面幅を変えて実ブラウザで確認。表示切替を往復し値が保持される | 検証済 |
| B    | 全127画面の操作を分類。画面移動・入力・保存・外部操作の行き先を点検 | 各操作に期待結果と実結果、未接続理由がある                     | 未完了 |
| C    | 承認画面と既存の実機能の接続方法を整理し、業務単位で修正            | 仕入れ→検品→採寸→在庫→発送→会計の入力が同じ商品へつながる      | 未完了 |
| D    | PC・モバイルの全ページを再確認                                      | 到達性、クリック、入力、重なり、エラーと再試行、狭い画面の確認 | 未完了 |
| E    | 統合検証、独立レビュー、進捗記録                                    | 自動検証・ブラウザ証拠・Issue/Project・Obsidianを更新          | 未完了 |

## 担当・変更範囲

- 計画・実装・統合: ルートCodex。親の実モデル設定と費用は未確認。
- 読み取り監査: `interaction_audit`（Astra lowを要求して起動。実設定は担当側から未確認）。A限定の独立レビュー合格。最終差分レビューは別実行で行う。
- Aは承認PCホームCSS、モバイルLoginInputとその検証だけを変更する。
- B以降は既存 `approved-ui-runtime-route-map.md`、仕様、実機能を直接確認して具体化する。未承認の新しい保存先・有料ホスト・外部APIを追加しない。
- 未接続の保存を成功表示にすることや、一律トーストで「全機能完成」と扱うことを禁止する。
- 本計画は大きいGoalを小さな修正へ縮小するものではない。A合格だけではGoalを完了しない。

## 検証記録

- 初期証拠: `output/playwright/all-pages-pc-home-before.png`（公開版PC02）。
- 全体のボタン検証は未完了。過去の127画面の表示確認を操作合格へ読み替えない。

### 2026-09-20 パケットA・初回全画面点検

- A: PC02カード内の絶対配置を通常のグリッド配置へ変更。768/1024/1440pxでアイコンと金額の交差0、金額の右はみ出し0。同サイズ比較も `all-pages-pc-home-after-same-size.png` へ撮影。
- A: モバイル01で架空文字列を入力し、表示→非表示の往復と値保持を390×844の実ブラウザで確認。独立レビュー合格（全体の合格ではない）。
- 本番用確認版をブラウザで巡回し、PC52/52、モバイル75/75がHTTP 200、横あふれ0。操作要素を取得。押下と保存の全件合格ではない。
- PC48の「作成する」2箇所、PC50の保存先、PC51のCSV/バックアップは自己リンクで、業務処理未接続。
- モバイル画面一覧のNext Link先読みに404を再現。既存ホーム同様の通常リンクに統一し回帰テストを追加。新しい専用ブラウザで先読み404消失、一覧→01の移動を確認。独立レビュー合格。既存favicon.icoの404は残る（非重大）。
- 画面対応61テスト、reviewの型確認・build、lintは合格。全体check初回は580中2テストが5秒で時間切れ、当該38テストだけの再実行は合格。整形修正後の全体 `npm.cmd run check` は581テスト、security、Web/API buildまでexit 0。テストの制限時間や合格条件は緩めていない。
- Issue #11はOPEN、Project #1はIn Progress。Obsidianの2026-09-20作業記録は保存・読み戻し済み。

### 次パケットの境界

- 採寸は `workflow-live-layout.tsx` の `WorkflowCaptureSteps` のprops境界で承認UIへ統合できる。`p0-workspace.tsx` の既存確認・保存・再試行境界を維持する。
- 検品は接続先APIが未実装。`p12-product-template-proposal-v1.md` の写真必須/任意とパンツ/スカート分岐が利用者判断待ち。2026-09-20に2点を非同期で再確認した。回答を採用記録へ残してから別パケットのAPI/DB/Web実装を進める。
- 既存APIはPC内のサーバー用であり、GitHub Pages内で動くと偽らない。未承認のクラウド保存・認証変更をしない。

### 2026-09-20 採寸の実機能側・表示改善

- `WorkflowCaptureSteps` の実際に入力する採寸画面を、PCでは写真/入力の2列、モバイルでは写真→大きい入力欄の1列に整理した。定義ID、数値、根拠写真、保存関数、未入力時の停止条件は維持。
- 架空データの一時ハーネスで実コンポーネントを動かし、肩幅47.5と根拠写真を入力→身幅へ進む（空欄/次へ無効）→戻る（47.5保持）をブラウザ確認。390/1440pxで横あふれ0。DB保存試験ではない。
- 一度の自動確認では画面更新を待たずに次の操作を行いタイムアウト。更新後の見出しを待つ再確認で上記操作は合格。
- 独立レビュー合格。モバイルのDOM順と表示順の差、実iPhoneキーボード表示時の入力欄/フッター関係は引き続き確認対象。承認モック全ページ完全一致を主張しない。
- 一時ハーネスは削除し、生成済みの古い型情報を除去してreview buildを再合格。公開用出力へ検証専用ページを含めない。
- 証拠: main側 `output/playwright/measurement-live-pc.png`、`measurement-live-mobile-final.png`。
- この差分に対する全体checkは581テスト、Web/API buildを含めexit 0。

### 2026-09-20 続行・操作順の修正

- 採寸の写真選択を数値入力よりDOM上でも前へ移し、モバイルの表示順とキーボード操作順を一致させた。状態更新・保存条件・CSSは変更なし。回帰テストを追加し、対象10件合格。別担当の読み取りレビューも合格。追加差分後の実Tab移動とiPhoneキーボードは未確認。
- 既存のPostgreSQL一時フォルダを調査したが、調べた配置にはpostgres/initdb/pg_ctl実行ファイルが残っておらず、実行中プロセスも検出されなかった。既存DBデータは変更していない。次の保存確認には隔離した検証DB環境の復旧が必要。外部クラウドを代用しない。
- 操作順追加後の全体checkもexit 0（session 22089）。582テスト、整形・lint・型・security・Web/API build合格。Goal全体の完了条件は未達。

### 次の集中修正: 商品カードの行き先

- 読み取り監査で、ホームの商品別「この作業を開く」がSKUを渡さず、通常業務画面が先頭商品を選ぶ問題を検出した。
- 限定実装担当: interaction_audit（既存担当）、独立確認: root別実行。対象はhome-workspaceのworkflowリンク、workflow/pageのsearchParams受け渡し、P0Workspaceの初期選択、関連回帰テスト。保存API・権限・pilot制約・shipping/inventory経路は変更しない。
- URLの商品指定はサーバーから取得した権限内の商品リストでのみ解決する。不明な指定を別商品へ黙って置き換えない。先頭商品以外を開く・存在しない商品指定・通常の指定なしを検証する。
- rootは並行して隔離DBとブラウザを準備し、この担当範囲を編集しない。「＋仕入れ」自己リンクは別候補として残す。

### 2026-09-20 実保存環境復旧・商品指定

- EDB公式のPostgreSQL 18.6 ZIPから専用Tempへ本体を展開。127.0.0.1:55439だけで起動し、新規DBへ現行migrationと既存UI評価fixtureを投入。以前のDBは変更なし。通常APIは制限LOGINを使用。
- 商品別workflowリンクがSKUを渡すよう修正。rootが差分・選択helper・回帰テストを独立確認。対象22テスト、型・lint合格。通常商品指定の限定レビューは合格。進行中pilotと別SKU URLの組合せは未確認として残す。
- 実ブラウザで先頭でない商品指定、不明SKUの明示エラー、指定なしの既存先頭選択を確認。格納後のホーム商品カードからも対象トップスが選択されることを確認。
- ブラウザで保管場所を登録→reloadで残存→商品番号0001と場所コード照合→確認保存→商品別カード→掲載用写真4枚→採寸4項目→保存→reload→採寸まとめの再表示が成功。値は肩幅47.5、身幅52、着丈61、袖丈70cm。全て架空データ。画面内メモリだけでなく実DB保存の検証。
- 採寸の写真からTabで数値欄へ移る実操作は合格。一方、数値欄へフォーカスしたとき下部メニューに欄が隠れる状態を390×844で撮影した。次の集中修正対象。実iPhoneキーボードは未確認。
- 証拠: `live-location-persisted.png`、`live-measurement-focused-mobile.png`（未修正の重なり）、`live-measurement-persisted-mobile.png`。main側output/playwright内。
- 全体check: session 66830がexit 0。591テスト、整形・lint・型・security・Web/API build合格。全ページ完成・公開反映は未達。
- Obsidianは最新保存試験まで追記・読み戻し済み。Issue #11への今回結果の更新はauto-reviewが具体的公開権限不足として拒否。送信は実施されていない。下書きはmain側output/all-pages-issue.mdに保存。対象と内容のユーザー確認後に最新本文を再取得して再開する。外部更新だけの停止で、コード修正は続行可能。

### 2026-09-20 フォーカスの重なり・番号確認の読込競合

- rootが採寸を含むworkflow操作要素へモバイル限定scroll-margin-blockを追加。別実行interaction_auditが限定差分をレビューし合格。390×844、390×500、320×568の実ブラウザで、写真からTab→数値欄の上下端がヘッダーとフッターの間に収まり、横あふれ0。実iPhoneのソフトキーボードは未確認。
- 別の架空商品0002を読み取る試験で、一覧取得前に押すと「有効な未格納の商品が見つかりません」と誤表示した。同じ番号を取得後に押すと成功し、読込競合と判定。
- interaction_auditがmobile-scan-workflowと関連testだけを修正。読込status、未準備時の手動/バーコード判定停止、取得失敗の再試行、応答schema検査、AbortControllerと遅延応答の破棄を実装。rootが差分を独立確認し合格。商品/場所の二重照合・格納保存条件は変更なし。
- ブラウザ上で一覧通信を保留すると確認ボタン無効、誤った未検出表示0、通信再開後に同じ番号で場所確認へ進んだ。503を注入した別試験は再読込ボタン表示→通信復旧→入力0003保持→場所確認へ進んだ。注入した503以外の障害は観測なし。撮影カメラ自体は未検証。
- 証拠: main/output/playwright/live-measurement-focus-fixed-mobile.png、live-scan-retry-mobile.png。対象workflow10/scan16テスト合格。全体checkはsession94361がexit 0、593テストとWeb/API buildまで合格。
- GitHub更新は未回答の公開権限確認待ち。拒否された送信を迂回していない。Goal全体は継続中。

### 2026-09-20 新規仕入れの入口

- [x] 「＋仕入れ」「仕入商品を登録」が既存商品の工程へ戻る問題を修正。明示した新規URLで既存商品の選択を切り離す。商品指定URLとの同時指定は商品指定を優先。
- [x] 新規登録の前に試験計測の状態取得を待ち、通信失敗時は再試行、進行中なら通常商品の新規登録を停止する。
- [x] 架空商品1件を実ブラウザで登録し、商品番号発行→同じSKUへのURL置換→再読み込み後も同じ商品表示を確認。既存の再送対策は維持。
- [x] 通信遅延・503・再試行・進行中の最小応答をブラウザで注入し、未確認/試験中に登録フォームが表示されないことを確認。応答置換は解除済み。実際の試験計測開始/完走はこの確認に含まない。
- [x] PCの実際の「＋仕入れ」リンクから遷移。1440/390pxで横はみ出しなし。画像はmain/output/playwright/live-new-purchase-pc.png、live-new-purchase-mobile.png。
- [x] interaction_auditの独立再レビュー合格。全体check session76767 exit 0、594テストとWeb/APIビルド合格。
- 全体完成・公開反映は未達。次は既存の出品準備から注文/発送までの実操作を点検し、未接続操作を限定修正する。検品仕様の本人判断待ちは継続。

### 2026-09-20 写真履歴による出品準備停止

- [x] 写真4種類があっても古いタグ写真2枚が残ると合計6枚になり、枚数完全一致の判定で確認ボタンが永久に無効になる不具合を実ブラウザで再現、修正。
- [x] APIのcreated_at/id昇順配列を根拠に各roleの最新を選択し、プレビュー・ダウンロード・4種類の存在判定を統一。古い履歴は消さず、採寸写真は掲載対象にしない。
- [x] 架空TOPSの属性確認保存→商品まとめ→確認してコピー→注文フォームまで成功。架空注文UI-ORDER-20260920-0245の保存も成功。外部出品なし。
- [x] 独立レビュー合格。全体check session83282 exit 0、597テスト、Web/API build合格。
- [x] 当時の優先問題だった旧progressOrderの直接操作は、後続の「発送導線の統合」で撤去済み。商品画面から対象注文を指定した専用ShippingWorkspaceへ移動し、遷移だけではPOSTしないことを実確認。安全確認は緩めていない。
- 専用/shippingは承認UIが表示され、登録済み注文の選択欄から今回注文を選択できる状態を確認。現在は新規注文フォームが初期表示されるため、単なるリンク置換では対象選択に注意が必要。

### 2026-09-20 発送導線の統合

- [x] 旧progressOrderと架空scan時刻、旧住所表示を撤去。商品画面から/shipping?order=対象へ移動する。移動時のPOSTが0件で、取り出し/発送を勝手に進めないことを実ブラウザ確認。
- [x] 専用発送画面は権限/担当期限確認後の注文だけを選ぶ。不明/空/重複指定は別注文へ切替せず停止。手動選択後もURLを更新し、reloadして対象を保持する。処理中切替・機密情報クリア・再認証保護を維持。
- [x] 実ブラウザで有効指定、未知/空/重複、手動選択/reload、取得応答の非先頭対象と対象消失時の停止を確認。応答置換は解除。全体check17588 exit0、601テストとWeb/API build合格、相互独立レビュー合格。
- [x] 架空注文で写真なしを人が今回だけ選択→梱包5項目確認→保存→発送画面への続行を確認。
- [x] 当時残っていた旧P0注文作成経路は、後続の「新規注文登録も現行画面へ統合」で撤去済み。未受注商品は現在の注文登録へ進み、既存legacy注文の互換APIは無条件に壊していない。
- ビルド中のAPI再起動で一度ブラウザ試験が500/時間切れ。既存APIハンドル34400とport3221復旧を確認し同条件再試験合格。不要なサーバー再起動は行っていない。

### 2026-09-20 新規注文登録も現行画面へ統合

- [x] 旧P0 createOrderと旧入力formを撤去。未受注商品は/shipping?sku=対象で現在の注文登録へ。既存注文はorder指定導線を維持。
- [x] 認可済み候補のavailable/listing_confirmed/未受注条件を満たす一意のSKU/在庫だけを初期選択。販売先・住所扱いは未選択を維持。不明/空/重複/割当済みは別商品にfallbackせず停止。保存後order URLへ置換。
- [x] 架空OUTERを写真4枚・採寸48/55/70/62保存→属性→コピー確認→新注文へ引継ぎ。匿名/メルカリ選択、販売額未入力で登録、reload同じ注文、order+sku優先、割当済み再登録拒否を実ブラウザ確認。
- [x] 登録後の人による情報確認→商品/場所の実入力→取り出し保存→今回だけ写真なしを選択→梱包5項目保存→配送方法画面へ到達。送料一覧未設定で方法確定がdisabled、発送を飛ばさない。
- [x] PC1440px・mobile390pxの注文確認に横はみ出しなし。独立レビュー合格。check14095 exit0、605テスト/WebAPI build成功。
- 仕様確認: DECISIONS 2026-09-03とacceptance-mapの現行新規注文要件に統合。既存legacy注文のNULL互換やAPIは変更せず、未入力値を補完しない。旧保存データの移行完了とはしない。
- [x] 送料一覧設定→方法選択→欠損情報の人による確認→発送完了→会計への導線は後続で実操作済み。
- [x] 隔離DBへ架空の発送専任メンバーを作成し、架空パンツ注文だけを8時間割当。担当ログインでは割当注文を表示する一方、同じSKUを `/shipping?sku=...` で直接指定すると「この担当では新しい注文を登録できません」と停止し、割当注文へ自動切替しないことを実ブラウザ確認。保存系通信0件。PC幅と390×844で表示し、スマホ相当幅は `scrollWidth=innerWidth=390`。証拠 `live-shipping-role-sku-rejected-pc.png` / `mobile.png`。
- [ ] 実iPhoneでの同操作は未実施。ブラウザの390×844確認と実機確認を区別する。

### 2026-09-20 送料設定・発送完了・会計到達

- [x] 検証専用の架空配送方法750円（実利用禁止と明記）をUIで登録→方法選択→発送前確認→日時入力→発送記録成功。実配送サービスや外部出品は操作していない。日時空欄の発送ボタンは無効。
- [x] 送料欄をNumber変換すると空欄が0円になるコードを修正。数字の整数表記、非負、安全整数を要求し、明示0円は許可。21回帰テスト追加。
- [x] 保存中でも押せるのに閉じないcatalogボタンをbusy中disabled/保存中表示へ。Escapeとfocus復帰にもbusy guard。
- [x] 完了後Continueが完了注文を再取得して指定エラーになる現象を実証。completeかつ非busy時に/shippingへ戻すよう修正。これと保存中closeの修正後実ブラウザ再走は次の架空注文で実施する（現在注文は完了済み）。独立レビュー合格。
- [x] check76262 exit0、628テスト/WebAPI build成功。
- [x] 発送前のmissingInformationは後続で本人へ一覧表示し、注文・登録・配送方法・未入力一覧が変わると再確認を必須化。契約上の対象は販売先取引IDと販売金額の2項目であることも確認。
- [x] 会計の金額欠損409は、後続で不足/重複と権限/通信/サービス障害を分けて読込操作直下へ表示。CSV停止を維持。金額を補完してCSVまで完走する機能は別の判断待ち項目。

### 2026-09-20 未入力確認・会計の失敗理由

- [x] 発送前にサーバーが返す未入力項目（販売先の取引ID・販売金額）をPCとモバイルへ表示し、人のチェックを必須化。注文・登録内容・配送方法・未入力一覧が変わると確認を無効化。ボタンと送信処理の両方で未確認を停止。独立レビュー合格。
- 上の記録を訂正：発送前のmissingInformation契約は上記2項目だけ。販売手数料・梱包費はこの一覧には含まれない。
- [x] 会計の失敗理由を読み込み操作直下へ表示。金額不足/重複と権限/通信/サービス障害を区別し、古い金額や別注文の遅延応答を使わない。独立レビューと実ブラウザの409案内表示・CSV停止を確認。
- [x] 全体check19443 exit0、635テスト・整形・静的解析・型確認・Web/API build合格。
- [x] 発送確認欄のPC/モバイル表示、未チェック停止、チェック後有効、チェック解除で再停止を後続の実ブラウザで確認。
- [ ] 手数料・梱包費の安全な追加入力導線を仕様と照合する。現在は不足案内までで、会計処理の完了ではない。

### 2026-09-20 発送確認欄を実操作して表示修正

- [x] 架空在庫0005を格納→写真4枚と採寸4値保存→属性確認→注文登録→照合→梱包→配送方法→未入力2項目の確認まで実ブラウザで操作。
- [x] 未チェックで進めない、チェックで有効、外すと再び停止をスマホで確認。PCでもチェック操作とボタン有効化を確認。
- [x] PCの確認欄がカード下端で切れたため、カードを縦スクロール可能にし、PC用の文字・余白へ調整。スマホは固定操作欄に不透明背景を付け、本文の文字がボタンに透けて重なる問題を修正。独立レビュー合格。
- [x] 架空注文の発送日時入力→記録完了→「作業を続ける」で/shippingへ戻り、新規注文画面が表示されることを確認。前回の完了注文指定エラーは再発なし。
- [x] PC 1440×1000で送料一覧を開き、Tab 18回で見出し→閉じる→登録済み項目→入力欄→確認欄→保存→閉じるへ一周し、全操作がダイアログ内に留まることを確認。ダイアログ下端874px、保存ボタン下端853px、下余白20pxで切れなし。
- [x] PC幅1440×1000で送料保存通信をブラウザ内に1件だけ保留し、閉じるが「保存中…」、保存が「保存しています…」となって両方とも無効であることを画像確認した。保留POSTはAPI・DBへ転送せず、再読込後の読取APIは登録済み3件のまま、未送信名と773円は存在しない。通信ルート0件、console error 0件。証拠 `output/playwright/live-catalog-busy-pc-final.png`。以前の解除競合で隔離DBだけに残った「PC保存中確認（実利用禁止）」771円は履歴として残し、外部・公開・実データではない。
- [x] スマホ発送レビューを390×844で確認後、Safari相当の短い390×664では最下部の料金注意欄が固定操作欄へ35px隠れることを実ブラウザで再現。review段階だけ下余白を56px追加し、iPhone safe area（ホームバー用余白）と同量だけ操作欄・本文余白が増えるよう修正。修正後は390×664と320×568の最下部で注意欄―操作欄21px、操作欄―下部メニュー9px、横はみ出し0。390×844の初期構成は変更なし。証拠 `live-shipping-review-short-before-fix.png` / `live-shipping-review-short-after-fix.png` / `live-shipping-review-mobile-final.png`。
- [x] 初回独立レビューで、全発送段階と621〜767pxにも操作欄位置を変更する範囲超過を検出。review祖先と既存2 breakpointへ限定し、0 inset時の既存位置73px/97pxを維持。再レビューPASS。最終check52517は74ファイル・666テスト、整形・静的解析・型・coverage・秘密情報検査・依存関係検査・Web/API build（86 routes）まで合格。実iPhoneは別の未完了項目として維持。
- [x] 表示修正後のcheck69494 exit0、635テストとWeb/API build合格。変更差分と計画Markdownの整形確認も成功。

### 2026-09-20 棚卸しと会計の実装欠落調査

- [x] 検証用の空の棚で、棚卸開始→初回計数提出→差異0の確認→承認を実ブラウザで確認。PC1440×1000・スマホ390×844で横はみ出しなし。差異あり・復元・返品はこの試験の対象外。
- [ ] 現行登録注文では手数料・梱包費の追加入力API/UIがなく、販売額の追加APIも発送前専用。全ての必須金額をそろえられず、会計409から回復できない。これは会計完走を妨げる必須修正。
- [x] `accounting-missing-facts-packet.md` に不足補完の設計条件・安全境界・完了試験を作成。独立のDB/権限/競合設計レビュー中。実装済みとは扱わない。
- [x] 後続で通常画面へ棚卸し対象の選択を追加し、承認済み履歴・復元画面・新規開始を選べることをPC/スマホで確認。

### 2026-09-20 承認後の棚卸し履歴へ戻る入口

- [x] 通常画面へ棚卸し対象の選択を追加。承認済みの履歴と復元画面を選べ、新規開始へも戻れる。承認待ち専用入口の既存優先選択は維持。
- [x] 対象切替で保留確認・証拠・差異選択を消去。独立レビュー指摘により計数フォームも棚卸しIDごとに初期化し、別対象へ未送信番号を持ち越さない。
- [x] PC/スマホで先ほど承認した差異ゼロの履歴表示と新規開始へ戻る操作に成功。独立再レビューPASS。差異ありの復元保存は別途未確認。
- [ ] 会計の税区分確認履歴・0円CSVの扱いは仕様判断が必要。質問済みで回答待ち。詳細は会計不足補完パケットを参照。

### 2026-09-20 返品から通常棚へ戻す流れ

- [x] 架空の発送済みTOPSを返品受取→商品番号照合→専用場所未登録の案内→保管場所登録→返品作業再開まで実操作。
- [x] 返品専用場所へ隔離→人の検品結果保存→通常棚へ再格納するリンク→商品/場所照合→保存まで成功。
- [x] 在庫画面を開き直してTOPSが「在庫あり」「UI-CAP-SHELF-4」、概要が在庫中1点であることを確認。検品直後は場所登録待ちであり、通常棚への格納を省略しなかった。
- [x] PC・スマホの返品結果画面を保存し、スマホの表示を目視。今回は正常系の既存機能確認でソース変更なし。廃棄候補側・競合・ネットワーク失敗は別途未確認。

### 2026-09-20 ラベルと保管場所写真

- [x] 架空商品2点を選び、異なるバーコードのラベルを表示。印刷用表示では操作メニューが除外されることを確認。実プリンターは使用していない。
- [x] 保管場所に架空写真をアップロードし、非公開保存・別担当の確認待ちを確認。撮影者本人の承認はAPIが拒否することを実操作で確認。
- [x] 本人には実行できない承認ボタンを表示せず、別担当の確認待ちを案内するよう修正。スマホ・PCで表示を確認。APIの自己承認禁止は維持。
- [x] 後続で別の架空管理者による写真承認、再読込後の状態保持、スマホ・PCで表示用画像のdecode/naturalWidthまで実ブラウザ確認。

### 2026-09-20 棚卸差異の再確認と復元

- [x] 進行中→承認済み履歴→進行中の対象切替で、未送信の商品番号が残らないことを実ブラウザ確認。
- [x] 架空在庫を初回計数で未読取→不足候補→商品/場所の照合→写真と理由→3秒確認→不足確定→再照合→復元まで実操作。PC・スマホの復元履歴を確認。
- [x] 不足確定直後に以前の照合済み入力が復元フォームへ残る問題を修正。差異の状態変更時に入力を初期化する。別の棚卸しで再走し、両番号の空欄と復元準備ボタン無効を確認後、再照合から復元成功。
- [x] 限定差分の独立レビューPASS。全体checkは638テスト・Web/API buildまで合格。
- [x] 3秒確認時の通信失敗からの再試行と、別担当へ引き継ぐ2名運用の差異確認は後続で実ブラウザ確認。
- [ ] 実端末のカメラ読取は未確認。

### 2026-09-20 棚卸保存失敗後の再試行

- [x] 最終確認の通信へ一度だけ503を注入し、失敗後も「保存中」で無効のままになる問題を再現。
- [x] 確認処理の終了時にボタンを初期化し、再試行にも3秒確認を必須とするよう修正。保存先・権限・確認時間は変更なし。
- [x] 修正版で不足確定の503→キーボードの再確認→保存成功、復元の503→ポインター長押し再試行→復元成功を実ブラウザ確認。通信注入は解除済み。PC・スマホ幅の画面証拠を保存。
- [x] 限定差分の独立レビュー合格。
- [x] 2名運用は後続の別担当承認まで実確認。
- [ ] 実端末カメラと全ページの残る操作確認は引き続き未完了。

### 2026-09-20 メンバーと担当の保存・解除

- [x] 架空の撮影担当をUI登録。保存後パスワード欄が空になること、再読込でメンバーが残ることを確認。
- [x] ニットの撮影・採寸を8時間割当。担当者としてログインし当該商品だけを表示、格納商品や場所は別割当がないため0件のまま。
- [x] 担当者のPCホームリンクが担当外画面へ進む問題を修正。管理者以外は作業ホームへ移動し、作業ホームから既存の安全なログアウト処理を使える。担当者・管理者の両方で実確認。
- [x] オーナーの解除申請→申請者本人は承認不可→別の架空在庫管理者で承認→有効担当なし→撮影担当で再ログインし撮影割当なしまで確認。変更履歴の表示も確認。
- [x] 独立レビューPASS。全体checkは640テスト・Web/API buildまで合格。
- [x] 開いたままの担当画面への解除反映、差し戻し・再申請・却下、変更履歴CSV、場所写真の別担当承認は後続で実確認。表計算ソフトでのCSV表示だけは未確認。

### 2026-09-20 承認済み場所写真の表示

- [x] 架空の別管理者で写真承認を保存し、再読込後に確認済み状態が残ることを確認。
- [x] 写真の表示時にブラウザfetchの呼出し対象が変わりIllegal invocationとなる不具合を実再現。window.fetch経由の関数へ変更し、スマホ・PCで画像のdecodeとnaturalWidthを確認。
- [x] 権限再確認、no-store、画像URLの破棄は維持。限定独立レビューPASS、全体check641テスト・Web/API build合格。
- [ ] 承認前に画像を確認できない仕様矛盾：製品AC-047/技術TA-033は承認前派生・API取得禁止。一方で人物等の写り込みを審査する要求がある。別管理者限定の審査用プレビュー例外を利用者へ質問し回答待ち。未承認のAPI緩和なし。
- [x] 商品番号0001の検索で正しい商品・棚・在庫中を確認。「次の商品を探す」で前の商品表示が消えることを確認。検索は在庫を更新しない。

### 2026-09-20 担当変更の差し戻し・却下・CSV

- [x] 変更履歴の保存ボタンでCSVを実ダウンロード。最初の申請/承認2行の対象・状態・操作をファイルから確認。
- [x] 架空パンツの撮影担当を割当→在庫管理者の解除申請→別のオーナーがコメントを選んで差戻し。元の担当が有効のままを確認。
- [x] 元の申請者が理由を変更して再申請→別オーナーが却下→再読込後も元の担当が有効なことを確認。
- [x] スマホ・PCの履歴画面を保存、PC目視。再出力CSVの6行に申請・承認・差戻し・却下が含まれることを確認。
- [x] 開いたままの担当画面への解除反映は次項で確認済み。CSVの表計算ソフト実表示は未確認。

### 2026-09-20 採寸写真の再選択と担当解除後の保存

- [x] 採寸の次項目で同じ写真ファイルを選ぶと次へ進めない問題を実再現。写真入力を採寸項目ごとに作り直し、続く3項目で同じ検証写真を選択して進めることを実確認。
- [x] 撮影担当の画面を開いたまま、別ブラウザで担当解除を申請・別管理者が承認。元の画面から保存すると実APIが403で拒否し、古い写真・採寸入力と保存フォームが撤去されることを確認。担当解除後の新規保存は発生しない。
- [x] 403時の英語表示を担当範囲についての日本語案内へ修正。既存の権限拒否・入力撤去処理は維持。修正後は一回限りの403応答をブラウザに注入して日本語表示を確認し、注入解除後の再読み込みで割当なし表示を確認。
- [x] 両修正の独立レビューPASS。全体checkは643テスト・69ファイル、整形・静的解析・型確認・秘密情報検査・Web/API buildまで成功。
- [x] 送料一覧の保存中操作は後続で実確認。
- [ ] 実スマホのカメラ、全ページの残りの実操作、公開反映は未完了で、全体Goalは継続。

### 2026-09-20 注文に使える商品がない場合の案内

- [x] 管理者で注文画面を開き、商品候補がないと理由なしで保存ボタンが無効となる状態を確認。
- [x] 読み込み成功後に候補が0件の場合だけ、出品準備と格納が必要な旨と「商品の準備へ進む」リンクを追加。読み込み中・取得失敗には空一覧の案内を出さない。既存の注文条件は変更しない。
- [x] PC・スマホの画面証拠を保存。スマホ横あふれなし、作業画面へのリンクを実クリックして遷移成功。限定独立レビューPASS。
- [x] 全体check85767はexit0。644テスト・69ファイル、整形・静的解析・型・security・Web/API build合格。
- [x] 後続で新しい架空注文を通常手順で準備し、送料保存中の操作停止、503後の入力保持、再試行保存、再読込後の残存まで実確認。

### 2026-09-20 採寸写真の重複を送信前に案内

- [x] 同じ画像を肩幅・身幅に選ぶと途中のアップロードで409となる現象を実再現。DBの専用根拠写真制約に整合し、別々の画像にすると実保存成功。
- [x] Web業務画面と担当撮影画面の両方で、画像内容の一致を最初の送信前に検査し、重複した項目名を日本語で案内。画像名の変更では回避できない。DB制約は変更なし。
- [x] 架空パンツ5項目で重複を選択→日本語表示・POST送信0件→写真を選び直す→保存成功→再読込で出品準備に進める状態を確認。
- [x] 独立レビューPASS。check11358 exit0、649テスト・70ファイル、整形/lint/型/security/WebAPIbuild合格。
- [x] 過去に保存した画像との重複はDB制約を維持したまま、既知の制約メッセージと完全一致する場合だけ専用の日本語案内へ変換。他のDB制約、既存エラー、担当解除403を誤変換しない。
- [x] 限定差分の独立レビューPASS。API・Web・担当撮影の対象45テストと型確認に合格。全体check session75749もexit0、665テスト・74ファイル、整形・静的解析・型・秘密情報検査・依存関係検査・Web/API buildまで合格。
- [x] 管理者の業務画面で、2枚目の採寸写真通信だけを503にして1枚目をサーバーへ保存済みにし、再試行では現在選択中の写真同士を別画像にしたうえで、2項目目だけ保存済み1枚目と同じ画像に変更。実PostgreSQLが409で拒否し、画面へ専用日本語案内が出ることを確認。別画像への修正後は4採寸を正常保存し、模擬通信0件も確認。証拠 `live-saved-measurement-duplicate-japanese-pc.png`。
- [x] 担当撮影画面でも保存済み写真との重複経路を実ブラウザ確認。専用の架空トップス `UI-CAP-WORKER-DUP` を既存の架空担当へ8時間割当。2枚目の採寸写真だけ503にして肩幅写真を実DBへ保存し、再試行では現在選択中の4枚を別画像にしたまま身幅だけを保存済み肩幅写真と同じに変更。実API 409から専用日本語案内を表示した。身幅を未使用写真へ修正後、採寸4件201・capture確定200、担当一覧から消えるまで成功。模擬通信0件、390×844で横はみ出し0。console error 2件は意図した503/409のみ。証拠 `live-worker-saved-photo-duplicate-japanese-mobile.png` / `live-worker-saved-photo-duplicate-recovered-mobile.png`。

### 2026-09-20 送料一覧の保存中操作と再試行

- [x] 架空パンツの属性→説明文確認→注文登録→商品/棚照合→写真なし選択→梱包の必須5確認→配送方法まで実操作。外部出品なし。
- [x] 送料保存POSTを一時保留し、保存・閉じるボタン無効、Escで閉じないことを確認。503応答後は閉じるボタンが有効に戻り、760円などの入力が残ることを確認。
- [x] 模擬通信を解除し、同じ情報で保存を再試行して成功。再読込後の注文確認を経て登録済み760円の架空配送方法を再表示。PCの送料一覧にも残存、横はみ出しなし、保存していない時のEsc閉じるは成功。
- [x] 証拠画像: `live-catalog-busy-mobile.png`、`live-catalog-retry-saved-mobile.png`、`live-catalog-retry-saved-pc.png`。今回コード変更なし、直前649テスト合格版で確認。
- [x] PC幅ではTab順と下端の切れなしを後続確認。503後に入力772円とメモが残り、追加保存0件の応答試験も成功。証拠 `live-catalog-retry-retained-pc.png`。
- [x] PC幅で「保存中…」の無効表示を再取得。閉じる・保存の両ボタン無効、未送信の773円が保存されていないこと、模擬通信0件を確認。証拠 `live-catalog-busy-pc-final.png`。実端末カメラは別の未完了項目として残し、注文は梱包済み・未発送のまま。

### 2026-09-20 掲載写真4枚の実保存と拡張子修正

- [x] 未格納の架空商品 `UI-CAP-WORKER-DUP` で、正面・背面・ブランドタグ・品質表示の4リンクをPCから実保存。4 GETは200、4ファイルを開いて役割を目視し、採寸根拠4件のasset IDと重複0件を確認。
- [x] 390×844でも正面写真を実保存し、PC保存分とbytes・SHA-256が一致。PCは `scrollWidth=clientWidth=1425`、スマホ相当は `375=375` で、4リンクと戻るボタンは画面内。調査画面へ戻って再読込後も商品・在庫・写真ID・更新時刻は不変。
- [x] 実応答が `image/png` でも保存名を`.jpg`へ固定する不具合を発見。`download`属性はSKU-roleの拡張子なし名称に変更し、実MIME typeからブラウザが拡張子を付けるよう修正。実PNGは `.png`、ブラウザのJPEG確認は `.jpg`。写真bytesやAPI・権限は変更しない。
- [x] 対象4テスト、Web typecheck、対象Prettier、限定Astra low独立レビューPASS。最終 `npm run check` は74ファイル・669テスト、整形・lint・型・coverage・秘密情報検査・依存関係検査・API/Web build（86 routes）まで合格。
- [x] 証拠は `output/playwright/live-listing-photo-download-evidence.md`、`live-listing-photo-download-pc.png`、`live-listing-photo-download-mobile.png`、`live-listing-photo-return-mobile.png`、`live-listing-photo-returned-research-mobile.png` と保存画像5件。実iPhone/Safariのファイル名決定と実API経由JPEG保存は未確認として区別する。

### 2026-09-20 属性訂正履歴と検索候補の即時更新

- [x] 未確認の架空商品でブランド・サイズ・色・タグ写真を初回保存し、revision 1・訂正元なしを確認。色だけを訂正し、revision 2と初回confirmation IDへの参照をAPI応答・画面・再読込で確認。
- [x] 属性保存直後だけ検索語と質問文が古い空欄のまま残るP2を独立レビューで検出。属性confirmation IDが変わった時だけ調査候補を再生成し、同じrevision内の手動編集は保持するよう修正。
- [x] モバイルの検索入力とボタンを専用classだけ1列へ変更。390pxで各ボタンのclient/scroll幅・高さが一致し文字切れなし、ページ横はみ出し0。PCの既存2列配置は維持。
- [x] revision 3への追加訂正後、再読込なしで検索語・質問文が最新属性へ更新。手動追記、検索語コピー、質問文コピーの成功案内を確認し、コピー操作による通信0件を確認。
- [x] 独立再レビューPASS。最終check session88133は74ファイル・670テスト、整形・静的解析・型・coverage・安全確認・Web/API buildまで合格。証拠は `output/playwright/live-attribute-correction-evidence.md`。
- [x] クリップボード書込みをブラウザ内で1回だけ拒否し、手動コピー案内、225文字の入力保持、同ボタンの再試行成功、一時入力欄への貼付け完全一致を確認。置換と一時欄は削除し、route 0・console error 0。外部検索・出品確定は未実行。
- [x] 失敗案内の代替手順も実操作。質問文欄を全選択して手動コピーし、PC内の一時入力欄への貼付け225文字が完全一致。元質問文は不変、一時欄0件、通信0件。
- [x] 質問文を243文字へ手動編集してから検索語を変更しても、同じrevision内では編集済み質問文を保持。コピーボタンからの貼付けも243文字完全一致。
- [x] 再読込後は未保存の質問文追記だけが消え、保存済み属性から作る標準221文字へ戻ること、revision 3・根拠・訂正元は残ることを確認。業務POST 0件、route 0、console error 0。
- [x] 検索語だけが空白なら検索語コピー・外部検索を無効、質問文も空白なら質問文コピーも無効。文字を戻すと3操作が復帰し、編集中の通信0件、外部検索未実行を確認。
- [x] 390×844の空欄状態を画像保存し、横はみ出しなし。再読込後は標準221文字・revision 3へ復帰、route 0・console error 0。

### 2026-09-20 商品ラベル一覧の読込失敗と復帰

- [x] 390×844で商品一覧GETだけを一時503にし、日本語の失敗案内と「再読み込み」ボタンを確認。
- [x] 一時通信差し替えを解除して「再読み込み」から実GET 200へ復帰。商品0006を検索・選択し、手書きプレビュー1枚・在庫番号一致・V2を確認。
- [x] 実印刷・再発行・業務POSTなし。最後は選択0件・検索空欄・警告0件、横はみ出しなし、最終route 0・console error 0。
- [x] 証拠 `output/playwright/live-label-load-recovery-evidence.md` と失敗・復帰画像2枚を保存。今回ソース変更なし、直前の全体check 670テスト合格を維持。
- [x] 失敗画像を警告全文・再読み込みボタン・固定下部メニューが同時に見える位置へ差替え。警告下端543.72px、下部メニュー上端785.67pxで重なりなし。独立再レビューPASS。
- [x] 0006選択後に該当しない検索へ変えても、選択1件・0006/V2プレビューを保持。検索を戻すとチェック保持、選択解除で0件へ戻ることを確認。
- [x] 選択0件のバーコード任意モードで印刷ボタン無効。実印刷・再発行・通信なし、最後は手書きモード・検索空欄、console error 0・route 0。
- [x] 一覧GETだけを架空25商品へ一時置換し、24点選択で25点目が無効、プレビュー24枚、在庫番号重複0を確認。実DBへ商品追加なし。
- [x] 1点解除で25点目が有効になり、入替後も24枚・番号24種類を維持。実印刷・業務POSTなし。
- [x] 選択解除・一時置換解除・再読込後、実一覧へ復帰。架空25商品0件、検索空、選択0、警告0、console error 0、route 0。
- [x] 架空24点をバーコード任意モードへ切り替え、印刷用表示でラベル24枚・バーコード24個・番号24種類・3列×8行を確認。実印刷なし。
- [x] A4領域内に全24枚、重なり0組、バーコード全件に幅・高さあり。印刷用表示では操作部とサイドメニューを非表示。画像を目視し切れ・重なりなし。
- [x] 画面表示・スマホ幅・実一覧・選択0へ復帰。非GET通信0、console error 0、route 0。

### 2026-09-20 保管場所登録の必須入力

- [x] 場所コード・表示名を両方空で発行しても、場所コードの必須案内を表示し非GET通信0件。
- [x] 架空コードだけ入力して表示名を空にすると、表示名の必須案内を表示し非GET通信0件。
- [x] 390×844で両対象欄は固定下部メニューより十分上。前後の場所2件・ID集合不変、入力を消して在庫一覧へ復帰。横はみ出しなし、console error 0、route 0。
- [x] 証拠 `output/playwright/live-location-required-evidence.md` と画像2枚。新しい場所・ラベルは作成していない。
- [x] 場所登録POSTだけをブラウザ内で503にすると、修正前は失敗案内が出てもコード・表示名が消える問題を発見。送信処理を変更し、成功時だけformをリセットするよう修正。
- [x] 修正後は架空コード・表示名を保持し、発行ボタンが再度有効。再読み込みで警告0へ復帰し、前後の場所2件・ID集合不変。POSTはAPI・DBへ転送していない。
- [x] 画像 `live-location-create-503-error-mobile.png` / `live-location-create-503-retained-mobile.png` を目視。入力欄と操作部品の切れ・固定下部メニューとの重なりなし。一時通信置換0、最終console error 0。
- [x] 契約テストを追加し、最終 `npm run check` は74ファイル・671テスト、整形・lint・型・coverage・秘密情報検査・依存関係検査・API/Web build（86 routes）まで合格。
- [x] 限定独立レビューはP1/P2なしでPASS。指摘された成功時分岐も、POSTをAPIへ転送しない一時201で追加確認。
- [x] 親場所・コード・表示名・用途・保管条件・最大点数を変更して送信後、成功時だけ全項目が初期値へ戻ることを確認。前後の場所2件・ID集合不変、画像 `live-location-create-mock-success-reset-mobile.png`、route 0、console error 0。
- [x] 登録POST一時201の直後に一覧GETだけを一時503にすると、修正前は入力と再発行ボタンが残り、保存済みの可能性を区別できず二重登録を誘発する問題を確認。
- [x] POST成功後の一覧失敗を分離し、保管場所一覧へ移動・form初期化。「登録完了」「再登録不要」「在庫を再読み込み」を明示。再読み込みで警告0、POST再送0、前後の場所2件・ID集合不変。
- [x] 画像 `live-location-post201-refresh503-safe-mobile.png` を目視し切れ・重なりなし。追加の限定独立レビューP1/P2なし。最終checkは74ファイル・671テストと全gate合格。POST成功応答自体を失う条件は未確認として区別する。

### 2026-09-20 PC画面28 返信文の編集とコピー

- [x] 表示だけだった4つの返信テンプレートを編集可能にし、それぞれの「コピー」と下部の「文章をコピー」を実動作へ接続。下部ボタンは最後に選んだ返信文をコピーし、空欄時は無効にする。
- [x] 公式ページは本人が確認して開く外部リンクに限定。API、自動送信、自動値下げ、返信文の自動保存は追加していない。
- [x] 1536×1024で104文字の長文を入力し、末尾へスクロールできること、コピー内容の完全一致、成功案内と下部ボタンの重なり0、横はみ出し0をサイドパネルで確認。
- [x] 768×1024でも長文入力欄とコピーボタンが返信カード内に収まり、ページ横はみ出し0を確認。
- [x] 対象9テスト、Web型確認、対象ファイル整形確認に合格。広すぎる高さ指定を右側メモ欄だけへ限定し、独立再レビューはP1/P2なしでPASS。
- [x] 文書更新後の全体 `npm run check` は75ファイル・674テスト、整形・静的解析・型・coverage・安全確認・API/Web build（86 routes）まで合格。この項目はPC28の手動返信コピーだけを閉じ、P1全体や返信履歴保存の完了を意味しない。

証拠: `output/playwright/live-pc28-reply-copy-evidence.md`。768pxでの成功案内の重なり実測、コピー拒否時の実ブラウザ確認、返信履歴保存は未確認として区別する。

### 2026-09-20 PC画面27 新価格候補のコピー

- [x] 次画面へ移動するだけだった「この候補をコピー」を、承認モック右端の「新価格を入力」欄へ本人が入力した金額のコピーへ変更。粗利・原価・販売履歴など内部情報はコピーしない。
- [x] 空欄、0、負数、小数、不正なカンマ、安全な整数範囲外を拒否し、案内後は価格欄へ戻す。全角数字・正しいカンマは本人の入力意図を保って `4,200円` 形式にする。
- [x] 空欄案内と価格欄フォーカス、`4,20`拒否・clipboard不変、`４２００円`から`4,200円`の完全一致をサイドパネルで実確認。ボタン後も`/pc/27`に留まり、自動値下げ・保存・API・外部送信はない。
- [x] 1536×1024と768×1024で横はみ出し0、成功案内とボタンの重なり0、入力欄とボタンが画面内であることを確認。
- [x] 対象3ファイル・17テスト、Web型確認、対象整形確認に合格。フォーカス復帰と通知の通常配置も契約テストへ追加し、独立再レビューはP1/P2なしPASS。
- [x] 文書更新後の全体 `npm run check` は77ファイル・688テスト、整形・静的解析・型・coverage・安全確認・API/Web build（86 routes）まで合格。この項目はPC27の価格コピーだけで、価格保存、公式画面への反映、P1全体の完了を意味しない。

証拠: `output/playwright/live-pc27-price-copy-evidence.md`。ブラウザ側でクリップボード書込みそのものを拒否された場合の実操作は未確認として区別する。

### 2026-09-20 PC画面24 商品説明コピーと未実装操作の明示

- [x] 表示だけだった商品説明を編集可能な状態で本人操作のコピーへ接続。空欄では無効、3行の架空文を編集後にコピー内容の完全一致、再入力後の再コピーをサイドパネルで確認。
- [x] 「公式画面を開く」は公開URLを新しいタブで開く本人操作リンクへ変更。`noopener noreferrer`を付け、自動出品・API・外部自動送信は追加していない。実サイトは自動で開かずリンク属性だけ確認。
- [x] 初回独立レビューで、未実装の写真12枚保存が自己リンク、出品情報保存が保存せず次画面へ進むP2を検出。写真と出品情報の両操作を理由付き「準備中」・disabledへ変更し、保存した誤解を防止。
- [x] 1536×1024と768×1024で横はみ出し0。説明欄・コピー結果・公式リンク・準備中の理由とボタンが画面内で、切れ・重なりなし。両準備中ボタンの`disabled=true`を確認。
- [x] 対象3ファイル・9テスト、Web型確認、対象整形確認に合格。P2修正後の独立再レビューは新規P1/P2なしPASS。
- [x] 文書更新後の全体 `npm run check` は78ファイル・691テスト、整形・静的解析・型・coverage・秘密情報検査・依存関係検査・API/Web build（86 routes）まで合格。写真ZIP、出品情報の保存契約、clipboard API拒否時の実操作、PC24全機能、P1全体は未完了として区別する。

証拠: `output/playwright/live-pc24-official-handoff-evidence.md`。

### 2026-09-20 PC画面23→24 商品説明の確認フロー

- [x] PC23の自己リンクだった「プレビュー」「リセット」を実操作へ変更。文字数は編集に追従し、プレビューは本文だけを改行ごと表示、補足メモは混ぜない。
- [x] 商品説明と補足メモを同じブラウザータブ内だけで別々に保持。PC23の219文字をPC24へ完全一致で渡し、PC24で追記後に戻っても232文字とメモを復元。API・サーバー保存・自動出品・外部送信なし。
- [x] リセット前の確認と取消保持、空欄時の移動停止・理由表示・説明欄フォーカス、PC24のコピー219文字完全一致をサイドパネルで実操作。
- [x] 1536×1024と768×1024で横はみ出し0。プレビュー、説明欄、メモ、次へボタンの切れ・重なりなし。最終console error 0。
- [x] 初回独立レビューのP2（一時保持失敗を無視して戻ると編集消失）を修正。保存領域取得例外・書込み失敗を案内し、未保存時の戻るを停止。対象13テスト・Web型・整形に合格し、独立再レビューPASS。
- [ ] ブラウザーが一時保存を拒否した状態の実操作、実商品別サーバー保存、AI生成は未確認。PC24全機能やP1全体の完了とは扱わない。
- [x] 文書更新後の全体 `npm run check` は80ファイル・701テスト、整形・静的解析・型・coverage・秘密情報検査・依存関係検査・API/Web build（86 routes）まで合格。

証拠: `output/playwright/live-pc23-description-handoff-evidence.md`。

### 2026-09-20 PC画面25 保存商品ギャラリーの表示切替

- [x] 見た目だけだった一覧/ギャラリー切替と販売先A/B絞り込みを実動作へ接続。初期6件、販売先A 3件、販売先B 3件を表示する。
- [x] 見本6件なのに48件・24件・複数ページと表示していた不整合を除去。現在の絞り込みに合わせて `6件中1〜6件` または `3件中1〜3件` とし、存在しない前後ページ操作を無効化。
- [x] 商品URLが未登録なのに全商品をPC26へ移動していた誤動作を除去。「URL確認は準備中」「URL未登録」を表示し、商品ページボタンは無効。API・外部ページ・サーバー保存は追加していない。
- [x] サイドパネルでギャラリー→一覧→A→ギャラリー→B→全件の順に実操作。1536×1024と768×1024で横はみ出し0、最終console error 0。
- [x] 対象2ファイル・5テスト、Web型、整形、Astra low独立レビューに合格。レビューでP1/P2相当の指摘なし。
- [x] 全体 `npm run check` は82ファイル・706テスト、整形・静的解析・型・coverage・秘密情報検査・依存関係検査・API/Web build（86 routes）まで合格。全体負荷で5秒を僅かに超えた既存scryptテストは、内容を変えず該当1件の上限だけ10秒へ変更し、独立レビューPASS。
- [x] 認証済み `/workflow` へ、実際の商品URLの1回登録・永続保存・本人確認後の公式商品ページ表示を実装。PC25の架空6商品へは接続せず、実SKUだけを扱う。
- [ ] PC26の販売状況保存、写真ZIP、出品情報保存、P1全体は未完了として区別する。

証拠: `output/playwright/live-pc25-gallery-controls-evidence.md`。

### 2026-09-21 PC画面26 販売状況の確認

- [x] 見本商品6件を最終確認日の古い順に並べ、基準日 `2025-05-20` から5日・4日・3日・2日・1日・今日を算出して表示。
- [x] 商品選択で番号・商品名・販売先・9項目の下書きを切り替え、`ITM-0006=999`、`ITM-0005=222` の閲覧数が互いに混ざらないことをサイドパネルで確認。
- [x] 数字の直接入力とスクリーンショット候補表示を実動作へ接続。モードを往復しても選択商品と入力下書きを保持。
- [x] 商品URLと数値保存は理由付きの準備中表示と無効ボタンにし、成功・保存・遷移を装わない。画像はPC内だけの一時プレビューまで実動作へ接続し、API・外部送信・自動読取は追加しない。
- [x] 1536×1024と768×1024を実確認。768pxは `clientWidth=scrollWidth=768`、操作部品28件の画面外はみ出し0、最終console error 0。
- [x] 対象2ファイル・5テスト、Web型、対象ESLint、Astra low独立レビューに合格。全体 `npm run check` は84ファイル・711テスト、整形・静的解析・型・coverage・安全確認・API/Web build（86 routes）まで合格。
- [x] 再読込後も残る実商品URL保存と、本人確認後の公式ページ表示は認証済み `/workflow` へ実装。PC26の架空商品へ実データは混ぜない。
- [x] JPEG・PNG・WebP画像を本人が選び、商品ごとにPC内だけで一時表示できる。別商品へ移動して戻っても混ざらず、再選択・画面終了時に不要な一時URLを解放する。10MB、縦横10,000px、4,000万画素の上限を設けた。
- [ ] OCR、画像からの数値候補作成、数値のサーバー保存は未実装。基準日と6商品は画面確認用データで、P1全体の完了とは扱わない。

証拠: `output/playwright/live-pc26-sales-check-evidence.md`、`output/playwright/live-pc26-local-image-preview-evidence.md`。

### 2026-09-21 PC画面29→30 注文・取り出し見本の安全境界

- [x] 修正前に、PC29が販売先・販売金額の空欄でもPC30へ移動し、読取なしで固定「一致しました」と表示する架空成功をサイドパネルで再現。
- [x] PC29の住所説明を自己リンクから同一画面の開閉へ変更。4入力を画面内で保持し、取引ID・販売金額の未入力件数を入力に追従。「あとで」操作も未保存案内付きで実動作化。
- [x] 注文番号を表示見本と明示。実登録は理由付きdisabledにし、PC30の閲覧は別の「取り出し画面の見本を見る」リンクへ分離。API・DB・外部送信なし。
- [x] PC30の固定成功を一致状態の表示見本・実読取ではない案内へ変更。実完了は理由付きdisabled、PC31の閲覧は別の見本リンクへ分離。
- [x] 1536×1024と768×1024をサイドパネルで実操作。PC29/30とも横はみ出し0、768pxで操作部品の画面外0、登録・完了disabled、console error 0。
- [x] 新規1ファイル・4テスト、Web型、ESLint、整形、Astra low独立レビューに合格。全体 `npm run check` は85ファイル・715テスト、全gateとAPI/Web build（86 routes）まで合格。
- [ ] PC29の販売金額「必須」と「あとで確認・作業続行」の文言矛盾は利用者再承認待ち。共通上部メニュー、PC31以降、実iPhone/カメラは別項目として残す。

証拠: `output/playwright/live-pc29-30-preview-boundary-evidence.md`。

### 2026-09-21 PC画面31→32 梱包写真・発送見本の安全境界

- [x] PC31の写真方針を画面内選択へ接続し、カード枠・丸印・読み上げ状態を一致させた。「今回は使わない」も自己リンクではなく同じ画面内の設定変更にした。
- [x] 人の確認チェックは全項目未選択で開始し、選択数を追従表示。再撮影・写真保存は理由付きdisabledとし、見本から写真を保存・外部送信しないことを明示した。
- [x] PC32の固定「写真2枚・確認済み」「料金確定」を、架空注文・未確認・料金候補の見本表示へ変更。配送方法の選択、丸印、料金候補を画面内で連動させた。
- [x] 料金カタログ編集と発送記録は理由付きdisabled。PC32、PC33の閲覧は実保存と別の見本リンクへ分離し、API・DB・カメラ・永続保存・外部通信を追加していない。
- [x] サイドパネルで方針変更、確認チェック、使わない設定、PC32移動、配送方法B→C、料金 `¥450`→`¥300`、発送日時入力を実操作。保存・発送ボタンは無効、console error 0。
- [x] 1536×1024と768×512でPC31/32を確認。client/scroll幅・高さは一致し、操作部品の画面外はみ出し0。
- [x] 新規4テスト、発送関連7ファイル・49テスト、Web型、ESLint、整形、Astra low独立レビューに合格。全体 `npm run check` は86ファイル・719テスト、全gateとAPI/Web build（86 routes）まで合格。
- [ ] 実写真の撮影・保存、実注文の発送記録は静的見本では実装しない。認証済み `/shipping` を実業務の正本とし、実iPhone/カメラとPC33以降、共通上部メニューは別項目として残す。

証拠: `output/playwright/live-pc31-32-pack-ship-preview-evidence.md`。

### 2026-09-21 PC画面33→36 在庫見本の安全境界

- [x] PC33を架空データの表示見本と明示。保管場所を開く実操作と履歴表示を理由付きdisabledにし、誤って棚卸し画面へ移動する導線を除去。
- [x] PC34の固定オフライン保存・同期の説明を表示例へ変更。実棚卸し開始をdisabledにし、PC35閲覧を別リンクへ分離。
- [x] PC35の切替・再読取・写真追加・確認保存を理由付きdisabledへ変更。備考は見本画面内だけの未保存編集と明示し、PC36閲覧を別リンクへ分離。
- [x] PC36の仮状態・在庫復元・返品保留・確認保存を理由付きdisabledへ変更。固定長押し秒数と保存しない次画面遷移を除去。
- [x] PC33→34→35→36の見本閲覧導線をサイドパネルで実操作し、未実装の実処理とは分離されていることを確認。
- [x] 1536×1024と768×512でclient/scroll寸法一致、操作部品の画面外0、最終console error 0を確認。
- [x] 新規4テスト、関連66テスト、Web型、ESLint、整形、Astra low独立レビューに合格。全体 `npm run check` は87ファイル・723テスト、全gateとAPI/Web build（86 routes）まで合格。
- [ ] 実在庫・棚卸しは認証済み `/inventory` と `/inventory/stocktake` を正本とする。共通上部メニュー、PC37以降、実端末確認、P1全体は別項目として残す。

証拠: `output/playwright/live-pc33-36-inventory-preview-boundary-evidence.md`。

この時点のチェックリストは245/264項目（92.8%）。未実装の実保存や実端末確認も含めた製品全体の実用完成度は、今回4画面の誤操作表示を閉じたため約90%と見積もる。チェックリスト率とは別の指標であり、P1全体完了ではない。

### 2026-09-21 PC画面37→40 メンバー管理見本の安全境界

- [x] PC37を架空メンバーの表示見本と明示。招待・詳細・再送信・停止・次ページをdisabledにし、誤遷移と自己リンクを除去。PC38閲覧は別リンクへ分離。
- [x] PC38の担当・期間・閲覧権限を基本情報のみの表示例へ固定し、原価・住所の非表示説明と一致させた。担当割当保存をdisabled、PC39閲覧を別リンクへ分離。
- [x] PC39を架空申請と明示。コメント・差し戻し・承認をdisabledにし、PC40閲覧を別リンクへ分離。同一素材の写真配置見本を実4方向証拠と断定しない表示へ修正。
- [x] PC40を架空履歴と明示。検索・絞り込み・履歴出力・前後ページをdisabled。対応IDだけPC39へ接続し、異なる対象の誤詳細表示と無限ページを除去。
- [x] PC37→38→39→40の見本リンクをサイドパネルで実操作し、重要操作はすべて無効、最終console error 0を確認。
- [x] 1536×1024と768×512でPC37〜40のclient/scroll寸法一致、操作部品の画面外0を確認。PC39の2段化による画面外を実測で検出し4列1段へ修正済み。
- [x] 関連47テスト、Web型、ESLint、整形、Astra low独立レビューに合格。全体 `npm run check` は88ファイル・727テスト、全gateとAPI/Web build（86 routes）まで合格。
- [ ] 実操作は認証済み `/team` を正本とする。PC41以降、共通上部メニュー、実アカウント・複数端末確認、P1全体は別項目として残す。

証拠: `output/playwright/live-pc37-40-team-preview-boundary-evidence.md`。

この時点のチェックリストは252/272項目（92.6%）。未実装の実端末・複数利用者確認も含めた製品全体の実用完成度は、今回4画面の重大な誤操作表示を閉じたため約91%と見積もる。チェックリスト率とは別の指標であり、P1全体完了ではない。

### 2026-09-21 PC画面41→44 分析見本の安全境界

- [x] PC41を架空の見込み例と明示し、実績画面の閲覧を「販売後の実績の見本を見る」へ分離した。
- [x] PC42を架空の実績例と明示し、実際の締め集計ではない説明とPC43の見本リンクへ修正した。
- [x] PC43の月切替を理由付きdisabledにし、操作できない矢印を除去。PC44閲覧を別リンクへ分離した。
- [x] PC44の仕入先名・数値を架空の比較例と明示。不足データ確認をdisabledにし、PC45閲覧を別リンクへ分離した。
- [x] PC41→42→43→44をCodex右サイドパネルで実操作。重要な未実装操作は無効、最終console error 0を確認した。
- [x] 1536×1024と768×512で全4画面のclient/scroll寸法一致、操作部品の画面外0を確認した。
- [x] 関連42テスト、Web型、ESLint、整形、Astra low独立レビューに合格。全体 `npm run check` は89ファイル・731テスト、全gateとAPI/Web build（86 routes）まで合格した。
- [ ] 実分析ルート・実データ集計・集計定義・締め処理は別実装。PC45以降、共通上部メニュー、実アカウント・実端末、P1全体も残す。

証拠: `output/playwright/live-pc41-44-analytics-preview-boundary-evidence.md`。

この時点のチェックリストは259/280項目（92.5%）。未実装の実データ連携・実端末確認も含めた製品全体の実用完成度は、今回4画面の誤認表示を閉じたため約92%と見積もる。チェックリスト率とは別の指標であり、P1全体完了ではない。

### 2026-09-21 PC画面45→48 会計見本から実画面への引継ぎ

- [x] PC45の固定値を架空の表示見本と明示し、実データ確認は `/accounting?stage=format` へ接続した。見本画面内の更新・確定は行わない。
- [x] PC46の処理なし保存を除去し、実設定の変更・保存は `/accounting?stage=profile`、PC47の閲覧は別リンクへ分離した。ヘルプは画面内で開閉する。
- [x] PC47の件数を `5 / 3 / 1 / 1` に整合させ、採用・変更・保存は `/accounting?stage=mappings`、PC48の閲覧は別リンクへ分離した。
- [x] PC48の固定成功・確定表示を架空例と明示し、形式選択、ヘルプ、履歴・作成・ダウンロード・取込詳細への形式別リンクを実動作へ接続した。
- [x] `/accounting` が許可済みの段階と形式だけを受け取り、見本画面から実会計の該当段階へ直接移動できるようにした。
- [x] 未ログイン時の会計戻り先を安全に保持。入力時・URL正規化後の外部URL、`//`、バックスラッシュ、制御文字を拒否し、独立レビューで検出した2種類の外部転送問題を修正した。
- [x] Codex右サイドパネルでPC45→48、PC48の形式選択・ヘルプ・ログイン遷移を実操作。1536×1024と768×512で画面外操作部品0、PC48の切れ0、最終console error 0を確認した。
- [x] 関連5ファイル・40テスト、Web型、ESLint、整形、独立再レビューに合格。全体 `npm run check` は91ファイル・738テスト、全gateとAPI/Web build（86 routes）まで合格した。
- [ ] API・DBを起動した実アカウントでの保存、ファイル作成・ダウンロード・手動取込は未確認。PC49以降、共通上部メニュー、実端末・複数利用者、P1全体も残す。

証拠: `output/playwright/live-pc45-48-accounting-live-handoff-evidence.md`。

この時点のチェックリストは267/289項目（92.4%）。実データ連携・実端末確認を含む製品全体の実用完成度は約93%と見積もる。チェックリスト率とは別の指標であり、P1全体完了ではない。

### 2026-09-21 PC画面49→52 設定見本の安全境界

- [x] PC49を架空設定の見本と明示し、料金入力を読み取り専用、編集5件と保存を理由付きdisabledへ変更。PC50閲覧を別リンクへ分離した。
- [x] PC49の手数料表に「項目／手数料率（%）／最低手数料（円）」を追加し、承認デザインの意味と単位を復元した。
- [x] PC50の選択を画面内だけの見本と明示。フォルダー名を読み取り専用、選択・確認を理由付きdisabledにし、PC51閲覧を別リンクへ分離した。
- [x] PC50/52の欠けていた橙・緑・青の盾をSVG輪郭へ変更し、承認モックに近い形で表示した。
- [x] PC51のCSV／JSON選択、`aria-pressed`、保存・バックアップの形式名を連動させた。実ファイル作成は理由付きdisabledにした。
- [x] PC51の固定履歴を表示例、「完了」を「完了例」とし、実ファイル未作成を近接表示。PC52閲覧を別リンクへ分離した。
- [x] PC52を接続状態の表示例と明示し、実際の接続・料金を確認した結果ではない説明と各サービスの未接続例を表示した。
- [x] Codex右サイドパネルでPC49→52、保存先radio、書き出し・バックアップ形式を実操作。1536×1024と768×512で画面外操作部品0、切れ0、console error 0。対象10テスト、Web型、ESLint、整形、独立レビューに合格。全体 `npm run check` は92ファイル・743テスト、全gateとAPI/Web build（86 routes）まで合格した。
- [ ] 実料金設定、実保存先変更、全データ書き出し・バックアップ、実接続状態の取得はP1で未実装。共通上部メニュー、実アカウント・実端末・複数利用者、P1全体も残す。

証拠: `output/playwright/live-pc49-52-settings-preview-boundary-evidence.md`。

この時点のチェックリストは275/298項目（92.3%）。実データ連携・実端末確認を含む製品全体の実用完成度は約94%と見積もる。チェックリスト率とは別の指標であり、P1全体完了ではない。

### 2026-09-21 PC画面1→52 共通ヘッダー操作

- [x] 通知、ヘルプ、担当者、コンパクトメニュー、作業者メニューを、承認デザインを保った実ボタンへ変更した。
- [x] メニュー5種類を共通パネルとして開閉し、通知・担当名は架空例、P1実機能は準備中と明示した。外部取得、保存、公開、権限変更は行わない。
- [x] P0の実業務がある画面だけ許可済みの内部ルートを案内し、P1見本から未実装処理を実行済みに見せない。
- [x] パネル外クリック、閉じる、Esc、キーボードの操作位置移動と元ボタンへの復帰を実装した。独立レビューで初回表示と切替時の不足を検出し修正した。
- [x] PC2、13、21、29、33、52の代表レイアウトでサイドパネルから実クリックし、メニューと説明の表示を確認した。
- [x] PC1〜52を768×512で巡回し、共通操作がすべて `BUTTON`、画面外操作部品0、横スクロール0、巡回失敗0を確認した。
- [x] 初回巡回で検出したPC18、19、25、26、27、28の右端切れとPC25の下端超過を修正し、再巡回で0件を確認した。
- [x] 対象2ファイル・9テスト、Web型、ESLint、整形、独立再レビューに合格。全体 `npm run check` は93ファイル・751テスト、全gateとAPI/Web build（86 routes）まで合格した。

証拠: `output/playwright/live-pc-common-header-actions-evidence.md`。

この時点のチェックリストは283/306項目（92.5%）。実データ連携・実端末確認を含む製品全体の実用完成度は約95%と見積もる。会計不足補完、実アカウント・複数端末・実iPhone、P1実保存、全127画面の最終操作合格は未完了で、全体Goalは継続する。

### 2026-09-21 モバイル75画面の操作・表示監査

- [x] `mobileScreenIds` と比較用route mapから、数字01〜49と名前付き追加26画面の正しい75 URLを確定した。単純な50〜75ではなく、正本URLで404 0件を確認した。
- [x] 正本75画面を390×844で巡回し、横スクロール0、画面外操作0、名前なし操作0、巡回失敗0を確認した。
- [x] 全75画面に操作要素があり、押せそうな非操作要素0、初期表示の残留ダイアログ0を確認した。
- [x] 時刻、Dynamic Island、バッテリー等のiPhone状態表示の再現0、最終console error 0を確認した。
- [x] コード監査で有効だが無反応のボタン18件を検出。承認済み初期デザインを保ち、安全案内または表示内容の実コピーへ接続した。
- [x] P0操作は `/workflow`・`/shipping` へ案内し、P1の編集・画像選択・公式価格は準備中、保存・外部送信なしと明示した。
- [x] 案内のTab／Shift+Tab循環、Esc・閉じる後の元ボタン復帰、背景暗転をサイドパネルで実操作。独立レビューのP2/P3を修正した。
- [x] 対象2ファイル・36テスト、Web型、ESLint、整形、独立再レビューに合格。全体 `npm run check` は94ファイル・755テスト、全gateとAPI/Web build（86 routes）まで合格した。

証拠: `output/playwright/live-mobile-75-action-audit-evidence.md`。

この時点のチェックリストは291/314項目（92.7%）。実データ連携・実端末確認を含む製品全体の実用完成度は約96%と見積もる。実iPhone・カメラ・ファイル選択、P1実保存、会計不足補完、実アカウント・複数端末、全体Goalは未完了。

### 2026-09-21 公開確認版 全127画面の最終表示・操作境界監査

- [x] 公開確認版だけを判定する環境境界を追加し、実データ用画面へのリンクをWeb本体と分離した。
- [x] PC・モバイル共通案内と直接リンクの両方で、公開確認版から実業務ルートへ移動しないようにした。
- [x] モバイル75/75画面を静的な公開確認版・390×844で巡回し、404、横はみ出し、画面外操作、名前なし操作、自己リンク、実業務リンク、重なりを0件にした。
- [x] PC52/52画面を静的な公開確認版・768×512で巡回し、404、横はみ出し、画面外操作、名前なし操作、自己リンク、実業務リンク、安全指定のない外部リンク、重なりを0件にした。
- [x] PC7のブランド、特徴・カテゴリ、販売可否、メモへ具体的な操作名を付けた。
- [x] PC12の現在地を「倉庫」として示し、備考欄とチェック欄をカード内へ収めた。備考欄の四隅で操作対象が備考欄自身になることも実測した。
- [x] PC49の送料・手数料6入力へ、行と列が分かる具体的な操作名を付けた。
- [x] 開発画面だけのNext.js確認ボタンをアプリの重なりから除外し、静的な公開確認版で重なり0件を再確認した。
- [x] 最終 `npm run check` は98ファイル・770テスト、整形、静的解析、型、安全確認、API/Web本番ビルドまで合格した。
- [x] `/seller-assistant` 用の公開ビルドで134静的ページを生成し、出力HTMLの実業務リンクと `data-live-route` が0件であることを確認した。
- [x] Astra lowの独立レビューが公開境界とPC7・12・49を直接確認し、新規P1/P2 0件でPASSした。
- [ ] 実iPhone・カメラ・ファイル選択、実アカウント・複数端末、P1実保存、会計不足補完は未完了。公開確認版の全127画面合格と、製品全機能の完成は分けて扱う。

証拠: `output/playwright/static-all-pages-final-audit-evidence.md`。

この時点のチェックリストは302/326項目（92.6%）。全127画面の静的な公開確認版は表示・初期操作境界で合格したが、実端末と実データ保存を含む製品全体の実用完成度は約96%のまま。全体Goalは継続する。

### 2026-09-21 公開後の商品URL保存 開始契約

- [x] 承認済み仕様を再照合し、個人メルカリ、1回登録、本人確認、自動取得なしを今回の固定範囲とした。
- [x] 既存 `marketplace_reference` は相場根拠であり、出品確認後は更新不可のため流用しないと確認した。
- [x] PC24〜26は架空データの公開確認版として維持し、実SKUの保存先は認証済み `/workflow` に限定した。
- [x] 訂正、他販売先、まとめ売り、自動URL確認を対象外とし、未承認仕様を追加しない停止条件を固定した。
- [x] 新規0047、契約、API、実DB、実運用Webを実装し、保存・再読込・本人クリック用リンク属性の証拠をそろえた。外部ページ自体は開いていない。
- [x] full check、fresh/upgrade PostgreSQL、右サイドパネルの1440px／390px確認、別Astra medium独立レビューを合格させた。

開始契約: `docs/implementation/product-url-persistence-packet.md`。このパケットの完了だけで、実iPhone、会計不足補完、P1全体、全体Goalを完了扱いにしない。

### 2026-09-21 公開後の商品URL保存 完了証拠

- 初回ブラウザ確認でPWA中継の許可漏れによる404を再現し、UUID付き完全一致パスのGET/POSTだけを許可。回帰テストでPUT/PATCH・不正UUID・余分な階層を拒否した。
- 隔離PostgreSQL 18.6の新規fresh DBで46 migration・66-table RLSをPASS。別の空upgrade DBも0001〜0047をPASS。既存UI確認用DBは無変更。
- 架空商品へ `m987654321` を右サイドパネルで保存し、再読込後も同一ID・URL・本人確認日時を表示。DB保存行1、対応監査1、監査へのID/URL漏えい0。
- 390×844と1440×900で横あふれ0、console error/warning 0。外部ページは開かず、正しい `href`、`target="_blank"`、`rel="noopener noreferrer"` を確認した。
- 最終 `npm run check` は104ファイル・792テストと全gate、API/Web本番ビルドをPASS。別Astra mediumの独立レビューもP1/P2 0件でPASS。

証拠: `output/playwright/live-published-product-page-persistence-evidence.md`。

この時点のチェックリストは310/333項目（93.1%）。実商品URLのサーバー保存を閉じた一方、実iPhone・カメラ、会計不足補完、PC26の販売状況保存、複数端末などは残るため、製品全体の実用完成度は約96%のままとし、全体Goalを継続する。

### 2026-09-21 PC画面26 ローカル画像の一時プレビュー

- [x] 「画像を選ぶ」を実動作へ接続し、JPEG・PNG・WebPをPC内のブラウザーメモリだけで表示する。画像・ファイル名・内容をAPI、サーバー、外部サービスへ送らない。
- [x] 画像を商品単位で分離し、ITM-0006で選んだ画像がITM-0005へ表示されず、ITM-0006へ戻ると再表示されることを右サイドパネルで確認した。
- [x] 有効な画像の表示後だけ入力元を「本人が画像と比較して確認」へ変更する。OCR、自動入力、数値保存、外部ページ読取は行わない。
- [x] 10MB超、未対応形式、空ファイル、縦横10,000px超、4,000万画素超、表示失敗を日本語で拒否する。画像の差し替えと画面終了時は一時URLを解放する。
- [x] 1536×1024と768×1024を右サイドパネルで確認。768pxで横はみ出し0、画面外操作0、操作部品の重なり0、最終console error/warning 0。
- [x] 画像差し替え・削除・画面終了・古い読込通知の動作テストを追加。対象テストとWeb型・対象整形、全体`npm run check`（105ファイル・801テスト）、Astra low独立再レビュー（P1=0、P2=0、P3=0）に合格。
- [ ] OCR、画像からの数値候補、数値のサーバー保存、実iPhoneでの写真選択は別工程として残す。

開始契約: `docs/implementation/sales-check-image-preview-packet.md`。証拠: `output/playwright/live-pc26-local-image-preview-evidence.md`。

この時点のチェックリストは317/341項目（93.0%）。PC26の画像選択は安全なローカル一時表示まで閉じたが、OCR、販売状況保存、実iPhone、会計不足補完、複数端末などは残るため、製品全体の実用完成度は約96%のままとし、全体Goalを継続する。
