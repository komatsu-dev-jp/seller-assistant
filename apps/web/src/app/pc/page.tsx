import { ApprovedPcDemo } from "../../components/approved-pc/approved-pc-demo";

export const dynamic = "force-dynamic";

export default function ApprovedPcDemoIndexPage() {
  return <ApprovedPcDemo screenNumber={1} />;
}
