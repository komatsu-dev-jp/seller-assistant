# Copy this file to .env only when running the API locally.
# Do not commit actual values. The API fails closed while either value is blank.
DATABASE_URL=
SESSION_SECRET=
API_INTERNAL_ORIGIN=
APP_ORIGIN=
